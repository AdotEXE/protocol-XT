# Руководство по контрибьютингу

Спасибо за интерес к проекту Protocol TX! Мы рады вашему вкладу.

## 🚀 Начало работы

### Требования

- Node.js 20+ (LTS)
- npm или yarn
- Git
- Базовое понимание TypeScript и Babylon.js

### Настройка окружения

1. Форкните репозиторий
2. Клонируйте ваш форк:
   ```bash
   git clone https://github.com/YOUR_USERNAME/protocol-XT.git
   cd protocol-XT
   ```
3. Установите зависимости:
   ```bash
   npm install
   ```
4. Создайте ветку для вашей работы:
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📝 Процесс разработки

### Стиль кода

- Используйте TypeScript строгий режим
- Следуйте существующему стилю кода в проекте
- Используйте осмысленные имена переменных и функций
- Добавляйте комментарии для сложной логики
- Документируйте публичные API

### Структура коммитов

Используйте следующий формат для сообщений коммитов:

```
тип(область): краткое описание

Подробное описание (если нужно)

Closes #номер_issue
```

**Типы:**
- `feat` - новая функция
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - форматирование, отсутствующие точки с запятой и т.д.
- `refactor` - рефакторинг кода
- `perf` - улучшение производительности
- `test` - добавление тестов
- `chore` - обновление задач сборки, конфигурации и т.д.

**Примеры:**
```
feat(physics): добавлена система hover-физики для танков

fix(camera): исправлена тряска камеры при движении

docs(readme): обновлена документация по установке
```

### Тестирование

Перед отправкой PR убедитесь, что:

1. Код компилируется без ошибок:
   ```bash
   npm run build
   ```

2. Игра запускается без ошибок:
   ```bash
   npm run dev
   ```

3. Проверьте консоль браузера на наличие ошибок

## 🔍 Pull Request процесс

1. **Обновите вашу ветку** с последними изменениями из main:
   ```bash
   git checkout main
   git pull origin main
   git checkout your-feature-branch
   git rebase main
   ```

2. **Убедитесь, что код работает** и не ломает существующий функционал

3. **Обновите документацию** если вы добавили новую функцию или изменили существующую

4. **Создайте Pull Request** с подробным описанием:
   - Что было изменено
   - Почему это было изменено
   - Как это было протестировано
   - Скриншоты (если применимо)

5. **Ответьте на комментарии** ревьюеров и внесите необходимые изменения

## 🎯 Области для контрибьютинга

### Приоритетные задачи

- 🐛 Исправление багов
- 📚 Улучшение документации
- ⚡ Оптимизация производительности
- 🎨 Улучшение UI/UX
- 🧪 Добавление тестов
- 🌐 Локализация

### Новые функции

Перед началом работы над новой функцией, пожалуйста:

1. Создайте issue для обсуждения
2. Дождитесь одобрения от мейнтейнеров
3. Начните разработку

## 📋 Чеклист для PR

- [ ] Код следует стилю проекта
- [ ] Добавлены комментарии для сложной логики
- [ ] Документация обновлена
- [ ] CHANGELOG.md обновлен (если применимо)
- [ ] Код протестирован
- [ ] Нет ошибок компиляции
- [ ] Нет ошибок в консоли браузера

## 🐛 Сообщение о багах

При создании issue о баге, пожалуйста, укажите:

1. **Описание проблемы** - что происходит
2. **Шаги для воспроизведения** - как воспроизвести проблему
3. **Ожидаемое поведение** - что должно происходить
4. **Фактическое поведение** - что происходит на самом деле
5. **Скриншоты/видео** - если применимо
6. **Окружение**:
   - Браузер и версия
   - ОС
   - Версия Node.js
7. **Логи консоли** - ошибки из консоли браузера (F12)

## 💡 Предложения

Мы приветствуем предложения по улучшению! При создании issue с предложением:

1. Опишите проблему, которую это решает
2. Предложите решение
3. Обсудите альтернативы
4. Опишите преимущества и недостатки

## 📞 Вопросы?

Если у вас есть вопросы, создайте issue с меткой `question` или свяжитесь с мейнтейнерами.

## 🙏 Спасибо!

Спасибо за ваш вклад в Protocol TX! Каждый вклад, большой или маленький, помогает сделать проект лучше.

