# Список всех звуков в игре

## 🎵 ПОЛНЫЙ СПИСОК ЗВУКОВ

### 1. **Фоновые звуки (Ambient)**
- **`startAmbient()`** - Фоновый атмосферный звук (ветер, атмосфера)
  - Запускается автоматически при создании SoundManager
  - Тихий непрерывный шум с низкочастотным фильтром
  - Громкость: `ambientVolume = 0.2`

### 2. **Звуки меню и интерфейса (UI)**
- **`playIntroSound()`** - Звук при открытии меню
  - Кинематографический интро: восходящий синтезатор с басовым дропом
  - Вызывается при появлении главного меню
  - Громкость: `uiVolume = 0.5`

- **`playPurchase()`** - Звук покупки/успешного действия
  - Мажорное трезвучие (успешный аккорд)
  - Используется при покупке в гараже
  - Громкость: `uiVolume`

- **`playUpgrade()`** - Звук улучшения
  - Алиас для `playPurchase()` (тот же звук)

- **`playSuccess()`** - Звук успеха
  - Алиас для `playPurchase()` (тот же звук)

- **`playError()`** - Звук ошибки
  - Нисходящий диссонансный аккорд
  - Используется в чате при ошибках
  - Громкость: `uiVolume * 0.6`

- **`playWarning()`** - Звук предупреждения
  - Пульсирующий предупреждающий звук
  - Используется в чате
  - Громкость: `uiVolume * 0.5`

- **`playRespawn()`** - Звук респавна
  - Восходящий синусоидальный звук (350-700 Гц)
  - Громкость: `uiVolume`

### 3. **Звуки двигателя (Engine)**
- **`playEngineStartSound()`** - Звук запуска дизельного мотора
  - Реалистичный звук старта дизельного двигателя
  - Включает: стартер, прокрутку коленвала, компрессию, бас
  - Длительность: 3.5 секунды
  - Вызывается при нажатии "Play" в меню
  - Громкость: `engineVolume = 0.7`

- **`startEngine()`** - Звук работающего двигателя
  - Реалистичный дизельный мотор с множеством осцилляторов
  - Запускается через 2 секунды после `playEngineStartSound()`
  - Непрерывный звук, обновляется в зависимости от скорости
  - Громкость: `engineVolume = 0.7`

- **`updateEngine()`** - Обновление звука двигателя
  - Динамически меняет частоты и громкость в зависимости от скорости
  - Зоны скорости: idle, low, medium, high, max
  - Вызывается каждые 10 кадров из `tankController.ts`

- **`stopEngine()`** - Остановка звука двигателя

### 4. **Звуки выстрела и боя (Combat)**
- **`playShoot(cannonType, position, velocity)`** - Звук выстрела
  - Поддерживает разные типы пушек:
    - `"standard"` - стандартная пушка
    - `"heavy"` - тяжелая пушка (более низкий звук)
    - `"rapid"` / `"fast"` - быстрая пушка (более высокий звук)
    - `"sniper"` - снайперская пушка (самый мощный)
    - `"gatling"` - гатлинг (самый быстрый)
  - Громкость: `shootVolume = 0.75`
  - Поддерживает 3D позиционирование

- **`playExplosion(position, size, velocity)`** - Звук взрыва
  - Размер влияет на частоту и громкость
  - Громкость: `explosionVolume = 0.85`
  - Поддерживает 3D позиционирование

- **`playHit(hitType, position, velocity)`** - Звук попадания
  - Типы попаданий:
    - `"normal"` - обычное попадание
    - `"armor"` - попадание в броню
    - `"critical"` - критическое попадание
    - `"ricochet"` - рикошет
  - Громкость: `hitVolume = 0.55`
  - Поддерживает 3D позиционирование

- **`playRicochet(position)`** - Звук рикошета
  - Алиас для `playHit("ricochet", position)`

### 5. **Звуки механики танка (Tank Mechanics)**
- **`playReloadComplete()`** - Звук завершения перезарядки
  - Два механических клика (550 Гц и 750 Гц)
  - Громкость: `reloadVolume = 0.45`

- **`playMovement(speed, isTurning)`** - Звук движения гусениц
  - Шум гусениц зависит от скорости
  - Разный звук при повороте
  - Ограничение частоты: каждые 150 мс
  - Громкость: `movementVolume = 0.35`

- **`startTurretRotation(speed)`** - Звук вращения башни
  - Механический звук поворота башни
  - Частота зависит от скорости вращения
  - Громкость: `movementVolume * 0.3`

- **`updateTurretRotation(speed)`** - Обновление звука вращения башни
  - Динамически меняет частоту и громкость

- **`stopTurretRotation()`** - Остановка звука вращения башни

### 6. **Звуки гаража (Garage)**
- **`playGarageOpen()`** - Звук открытия гаража
  - Нисходящий пилообразный сигнал (220→90 Гц)
  - Громкость: `uiVolume`

- **`playGarageClose()`** - Звук закрытия гаража
  - Алиас для `playGarageOpen()` (тот же звук)

### 7. **Звуки подбора предметов (Pickups)**
- **`playPickup(pickupType?)`** - Звук подбора предмета
  - Приятный восходящий звук с гармониками
  - Три осциллятора для богатого звука
  - Громкость: `pickupVolume = 0.6`

---

## 🎯 КАКОЙ ЗВУК ИГРАЕТ ПЕРВЫМ ПРИ НАЧАЛЕ ИГРЫ?

### Порядок воспроизведения звуков при запуске игры:

1. **`startAmbient()`** - ⭐ **ПЕРВЫЙ ЗВУК**
   - Запускается автоматически в конструкторе `SoundManager`
   - Вызывается при создании объекта: `new SoundManager()`
   - Это фоновый атмосферный звук (тихий непрерывный шум)
   - Играет постоянно в фоне

2. **`playIntroSound()`** - Второй звук (опционально)
   - Вызывается при появлении главного меню
   - Кинематографический интро-звук
   - Длительность: ~1.5 секунды

3. **`playEngineStartSound()`** - Третий звук
   - Вызывается при нажатии кнопки "Play" в меню
   - Звук запуска дизельного мотора
   - Длительность: 3.5 секунды

4. **`startEngine()`** - Четвертый звук
   - Запускается через 2 секунды после `playEngineStartSound()`
   - Непрерывный звук работающего двигателя
   - Играет постоянно во время игры

---

## 📊 НАСТРОЙКИ ГРОМКОСТИ

```typescript
masterVolume = 0.7        // Общая громкость
engineVolume = 0.7        // Громкость мотора
shootVolume = 0.75       // Громкость выстрелов
explosionVolume = 0.85   // Громкость взрывов
hitVolume = 0.55         // Громкость попаданий
reloadVolume = 0.45      // Громкость перезарядки
movementVolume = 0.35    // Громкость движения
pickupVolume = 0.6       // Громкость подбора предметов
uiVolume = 0.5           // Громкость UI звуков
ambientVolume = 0.2      // Громкость фонового звука
```

---

## 📝 ПРИМЕЧАНИЯ

- Все звуки генерируются процедурно через Web Audio API
- Поддерживается 3D позиционирование для боевых звуков
- Звуки двигателя и башни обновляются динамически в зависимости от действий игрока
- Некоторые звуки являются алиасами других (например, `playUpgrade()` = `playPurchase()`)

