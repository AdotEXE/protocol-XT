# 🚀 DevTools MCP - Быстрый старт

## Шаг 1: Установка

```bash
npm install
```

## Шаг 2: Запуск Chrome с отладкой

В отдельном терминале:

```bash
npm run chrome:debug
```

Chrome откроется автоматически с поддержкой удаленной отладки.

## Шаг 3: Настройка Cursor

1. Откройте настройки Cursor (Ctrl+,)
2. Найдите раздел "MCP Servers"
3. Добавьте конфигурацию из файла `mcp-config.json`

Или скопируйте это в настройки MCP:

```json
{
  "mcpServers": {
    "devtools": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-devtools@0.11.0",
        "--browser-url=http://localhost:9222"
      ]
    }
  }
}
```

## Шаг 4: Готово! 🎉

Теперь в Cursor вы можете использовать DevTools MCP:

- "Проверь производительность страницы"
- "Покажи все проблемы из DevTools"
- "Проанализируй выбранный элемент"

---

📚 Подробная документация: [docs/DEVTOOLS_MCP_SETUP.md](docs/DEVTOOLS_MCP_SETUP.md)

