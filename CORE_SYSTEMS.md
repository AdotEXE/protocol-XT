# 🎮 Protocol TX - Основные системы и процессы

**Краткий обзор всех ключевых систем, логик и процессов проекта**

---

## 📋 Содержание

1. [Архитектура проекта](#архитектура-проекта)
2. [Игровой цикл](#игровой-цикл)
3. [Основные системы](#основные-системы)
4. [Процессы и логики](#процессы-и-логики)
5. [Сетевые системы](#сетевые-системы)
6. [Оптимизации](#оптимизации)

---

## 🏗️ Архитектура проекта

### Клиент-Сервер разделение

```
┌─────────────────┐         ┌─────────────────┐
│   CLIENT        │ ◄─────► │   SERVER        │
│   (Vercel)      │  WebSocket │   (Railway)    │
│                 │         │                 │
│ • Рендеринг     │         │ • Физика        │
│ • Ввод          │         │ • Валидация     │
│ • Предсказание  │         │ • Синхронизация  │
│ • UI            │         │ • Авторитет     │
└─────────────────┘         └─────────────────┘
```

**Клиент (src/client/)**:
- Рендеринг 3D (Babylon.js)
- Обработка ввода (клавиатура, мышь)
- Клиентское предсказание (smooth gameplay)
- UI и HUD

**Сервер (src/server/)**:
- Авторитетная физика (Havok)
- Валидация всех действий
- Синхронизация состояния (60Hz)
- Управление комнатами

**Общее (src/shared/)**:
- Типы данных
- Константы
- Утилиты
- Сетевые протоколы

---

## 🔄 Игровой цикл

### Инициализация

```
1. Game.init()
   ├─ Создание Scene и Engine
   ├─ Загрузка карты
   ├─ Создание систем (GameSystems.init())
   │  ├─ TankController
   │  ├─ HUD
   │  ├─ SoundManager
   │  ├─ EffectsManager
   │  ├─ EnemyManager
   │  ├─ ChunkSystem
   │  └─ ... (все системы)
   └─ Подключение к серверу (если multiplayer)
```

### Основной цикл (Game.update())

```
Каждый кадр (60 FPS):
├─ GameUpdate.update() - обновление всех систем
│  ├─ Camera: каждый кадр (критично)
│  ├─ Physics: каждый кадр (точность)
│  ├─ Tank: каждый кадр
│  ├─ HUD: каждые 6 кадров
│  ├─ Enemy AI: каждые 5-6 кадров
│  ├─ Chunk System: каждые 12-16 кадров
│  └─ ... (адаптивные интервалы)
├─ Рендеринг (Babylon.js)
└─ Синхронизация с сервером (если multiplayer)
```

---

## 🎯 Основные системы

### 1. **TankController** - Управление танком

**Что делает**:
- Управление движением (WASD)
- Hover-физика (парение над землей)
- Стрельба и баллистика
- Управление башней и камерой
- Респавн при смерти

**Ключевые компоненты**:
- `TankChassis` - корпус танка
- `TankWeapons` - оружие и стрельба
- `TankPhysics` - физика движения
- `TankInput` - обработка ввода

**Физика**:
- Havok Physics для коллизий
- Raycasts для определения высоты
- Anisotropic friction для реалистичного движения
- Upright forces для стабильности

---

### 2. **HUD** - Интерфейс игры

**Что делает**:
- Отображение здоровья, брони, патронов
- Мини-карта
- Цели и миссии
- Чат
- Статистика

**Компоненты** (`src/client/hud/components/`):
- `HUDManager` - главный менеджер
- `MinimapManager` - мини-карта
- `MissionPanel` - панель миссий
- `MultiplayerHUD` - HUD для multiplayer
- 25+ компонентов UI

**Обновление**: Каждые 6 кадров (не каждый кадр для производительности)

---

### 3. **EnemyManager** - Враги и AI

**Что делает**:
- Создание и управление врагами
- AI поведение (патрулирование, преследование, атака)
- Координация группы врагов
- Оптимизация (LOD для дальних врагов)

**AI Системы** (`src/client/ai/`):
- `EnemyAI` - базовое поведение
- `EnemyCombat` - боевая логика
- `AIPathfinding` - поиск пути
- `AICoordinator` - координация группы

**Обновление**: Каждые 5-6 кадров

---

### 4. **ChunkSystem** - Генерация мира

**Что делает**:
- Процедурная генерация карт
- Загрузка/выгрузка чанков (оптимизация)
- Генерация террейна, зданий, объектов
- Управление биомами

**Генераторы карт** (`src/client/maps/`):
- Urban, Wasteland, Canyon, Park, Snow и др.
- 8+ типов карт
- Noise-based генерация террейна
- Динамическая загрузка чанков

**Обновление**: Каждые 12-16 кадров (зависит от размера карты)

---

### 5. **MultiplayerManager** - Мультиплеер

**Что делает**:
- Подключение к серверу
- Синхронизация состояния (60Hz)
- Клиентское предсказание
- Обработка отключений

**Протокол**:
- WebSocket (TCP) - надежные сообщения
- Geckos.io (UDP) - быстрые обновления
- Binary protocol (msgpack) - эффективность
- Delta compression - минимум данных

**Синхронизация**:
- Позиции танков
- Здоровье и урон
- Выстрелы и попадания
- Состояние игры

---

### 6. **EffectsManager** - Визуальные эффекты

**Что делает**:
- Частицы (взрывы, дым, искры)
- Пост-обработка (эффекты экрана)
- Визуальные эффекты выстрелов
- Эффекты попаданий

**Ограничения**:
- Максимум 50 активных эффектов
- Автоматическое удаление старых эффектов
- Пулинг объектов эффектов

---

### 7. **SoundManager** - Звуковая система

**Что делает**:
- Воспроизведение звуков
- 3D позиционный звук
- Процедурная генерация звуков (jsfxr)
- Управление музыкой

**Звуки**:
- Выстрелы, взрывы, двигатель
- Звуки движения, попаданий
- UI звуки
- Голосовой чат

---

### 8. **CurrencyManager** - Экономика

**Что делает**:
- Управление валютой (кредиты)
- Начисление за убийства, миссии
- Покупка апгрейдов
- Сохранение в Firebase

---

### 9. **ExperienceSystem** - Опыт и прогресс

**Что делает**:
- Начисление опыта
- Уровни игрока
- Навыки и перки
- Прогрессия

**Связанные системы**:
- `PlayerProgression` - прогресс игрока
- `AchievementsSystem` - достижения
- `MissionSystem` - миссии
- `PlayerStats` - статистика

---

### 10. **DestructionSystem** - Разрушаемое окружение

**Что делает**:
- Управление разрушаемыми объектами
- Система здоровья объектов
- Визуальные эффекты разрушения
- Изменение укрытий

---

### 11. **Workshop** - Кастомизация танков

**Что делает**:
- Создание кастомных танков
- Выбор моделей
- Настройка параметров
- Редактор точек крепления

**Компоненты** (`src/client/workshop/`):
- `ConfigurationManager` - управление конфигурациями
- `ModelSelector` - выбор моделей
- `ParameterEditor` - редактор параметров
- `VisualEditor` - визуальный редактор

---

### 12. **Garage** - Гараж и апгрейды

**Что делает**:
- Система апгрейдов танка
- Покупка улучшений
- Предпросмотр изменений
- Сохранение конфигураций

---

## ⚙️ Процессы и логики

### Процесс стрельбы

```
1. Игрок нажимает ЛКМ
2. TankController.shoot()
   ├─ Проверка перезарядки
   ├─ Создание снаряда
   ├─ Расчет траектории (баллистика)
   ├─ Отправка на сервер (multiplayer)
   └─ Визуальный эффект
3. Сервер валидирует выстрел
4. Сервер создает снаряд на всех клиентах
5. Снаряд летит с учетом гравитации
6. При попадании:
   ├─ Расчет урона
   ├─ Визуальный эффект
   ├─ Звук попадания
   └─ Обновление здоровья
```

### Процесс движения

```
1. Игрок нажимает W/A/S/D
2. TankInput обрабатывает ввод
3. TankController.updateMovement()
   ├─ Расчет направления движения
   ├─ Raycast к земле (hover height)
   ├─ Применение сил (Havok Physics)
   └─ Обновление позиции
4. Сервер валидирует движение
5. Синхронизация позиции (60Hz)
6. Клиент интерполирует между обновлениями
```

### Процесс генерации карты

```
1. Выбор типа карты (Urban, Wasteland, etc.)
2. ChunkSystem.generateChunk()
   ├─ Генерация террейна (NoiseGenerator)
   ├─ Размещение зданий
   ├─ Создание дорог
   ├─ Размещение объектов
   └─ Оптимизация (LOD, culling)
3. Загрузка чанков по мере движения
4. Выгрузка дальних чанков
```

### Процесс синхронизации (Multiplayer)

```
Клиент:
1. Предсказание действия (immediate feedback)
2. Отправка на сервер
3. Получение ответа сервера
4. Reconciliation (коррекция при расхождении)
5. Интерполяция для плавности

Сервер:
1. Получение действия от клиента
2. Валидация (проверка возможности)
3. Применение к авторитетному состоянию
4. Отправка обновления всем клиентам
5. 60 обновлений в секунду
```

### Процесс респавна

```
1. Танк получает смертельный урон
2. TankController.die()
   ├─ Анимация смерти
   ├─ Эффекты взрыва
   └─ Вызов callback респавна
3. Получение позиции гаража
4. Создание нового танка
5. Анимация появления
6. Восстановление здоровья
```

---

## 🌐 Сетевые системы

### Архитектура сети

```
Client                    Server
  │                         │
  ├─ WebSocket ────────────►│  (надежные сообщения)
  │  • Чат                  │  • Команды
  │  • Команды              │  • Валидация
  │                         │
  ├─ Geckos (UDP) ─────────►│  (быстрые обновления)
  │  • Позиции              │  • Состояние игры
  │  • Действия             │  • Синхронизация
  │                         │
  └─ Binary (msgpack)       │  (эффективность)
     • Минимум данных       │  • Delta compression
```

### Типы сообщений

**От клиента к серверу**:
- `MOVE` - движение танка
- `SHOOT` - выстрел
- `CHAT` - сообщение в чат
- `JOIN_ROOM` - присоединение к комнате
- `LEAVE_ROOM` - выход из комнаты

**От сервера к клиенту**:
- `GAME_STATE` - состояние игры
- `PLAYER_UPDATE` - обновление игрока
- `PROJECTILE_CREATE` - создание снаряда
- `DAMAGE` - урон
- `CHAT_MESSAGE` - сообщение чата

### Синхронизация

- **Частота**: 60 обновлений в секунду (60Hz)
- **Протокол**: Binary (msgpack) для эффективности
- **Сжатие**: Delta compression (только изменения)
- **Интерполяция**: Плавное движение между обновлениями
- **Предсказание**: Клиент предсказывает действия для плавности

---

## 🚀 Оптимизации

### Адаптивные интервалы обновления

| Система | Частота | Причина |
|---------|---------|---------|
| Camera | Каждый кадр | Критично для плавности |
| Physics | Каждый кадр | Требуется для точности |
| Tank | Каждый кадр | Основной геймплей |
| HUD | Каждые 6 кадров | UI не нужен 60 FPS |
| Enemy AI | Каждые 5-6 кадров | AI не нужен 60 FPS |
| Chunk System | Каждые 12-16 кадров | Зависит от размера карты |
| Enemy Turrets | Каждые 15 кадров | Низкий приоритет |

**Адаптивность**: При FPS < 30, интервалы увеличиваются на 50%

### Кеширование

- **`getAbsolutePosition()`** - кешируется раз в кадр (80-90% снижение вычислений)
- **`computeWorldMatrix()`** - кешируется для избежания повторных расчетов
- **Raycasts** - кешируются когда позиция камеры не изменилась (> 0.5m)
- **Биомы** - кеш размером 50000 для быстрого доступа

### LOD (Level of Detail)

- **Враги > 150m**: Отключены детали (гусеницы, колеса, мелкие части)
- **Враги > 100m**: Упрощенная физика (ANIMATED вместо DYNAMIC)
- **Дальние объекты**: Упрощенные материалы
- **Результат**: 30-40% снижение нагрузки для дальних объектов

### Ограничения

- **Эффекты**: Максимум 50 активных эффектов
- **Материалы**: Пулинг и переиспользование
- **Физика**: Упрощенная для дальних объектов
- **Рендеринг**: Отключены тени, частицы, туман в production

---

## 📁 Структура файлов

### Основные файлы

```
src/client/
├── game.ts              # Главный класс Game
├── game/
│   ├── GameSystems.ts   # Управление всеми системами
│   ├── GameUpdate.ts    # Обновление всех систем
│   ├── GamePhysics.ts   # Физика
│   ├── GameMultiplayer.ts # Мультиплеер
│   └── ...
├── tankController.ts   # Управление танком
├── hud.ts              # Интерфейс
├── enemy.ts            # Враги
├── chunkSystem.ts      # Генерация мира
├── multiplayer.ts      # Мультиплеер менеджер
└── ...
```

### Системы по папкам

- `game/` - Игровые системы (GamePhysics, GameMultiplayer, etc.)
- `tank/` - Компоненты танка (Chassis, Weapons, Physics)
- `hud/` - UI компоненты (25+ компонентов)
- `maps/` - Генераторы карт (Urban, Wasteland, etc.)
- `effects/` - Визуальные эффекты
- `workshop/` - Кастомизация танков
- `garage/` - Гараж и апгрейды
- `ai/` - AI системы (EnemyAI, Pathfinding, etc.)

---

## 🔑 Ключевые концепции

### 1. **Server Authority** (Авторитет сервера)
Сервер - единственный источник правды. Все действия валидируются на сервере.

### 2. **Client Prediction** (Клиентское предсказание)
Клиент предсказывает действия для плавности, сервер корректирует при расхождении.

### 3. **Adaptive Updates** (Адаптивные обновления)
Системы обновляются с разной частотой в зависимости от важности.

### 4. **LOD System** (Система детализации)
Дальние объекты упрощаются для производительности.

### 5. **Object Pooling** (Пулинг объектов)
Переиспользование объектов для снижения нагрузки на GC.

---

## 📊 Производительность

### Целевые показатели

- **FPS**: 60 FPS (минимум 30 FPS на слабых устройствах)
- **Синхронизация**: 60 обновлений в секунду
- **Задержка сети**: < 100ms для комфортной игры
- **Память**: Оптимизация для предотвращения утечек

### Метрики оптимизации

- **Кеширование**: 80-90% снижение дорогих вычислений
- **LOD**: 30-40% снижение для дальних объектов
- **Адаптивные интервалы**: Автоматическая оптимизация при падении FPS
- **Production**: Отключены эффекты для максимальной производительности

---

## 🎓 Быстрый справочник

### Как добавить новую систему?

1. Создать класс системы
2. Добавить в `GameSystems.init()`
3. Добавить обновление в `Game.update()` с нужным интервалом
4. Добавить очистку в `dispose()`

### Как работает физика?

1. Havok Physics обрабатывает коллизии
2. Силы применяются в `onBeforePhysicsObservable`
3. Позиции читаются в `onAfterPhysicsObservable`
4. Havok автоматически синхронизирует mesh.position

### Как работает multiplayer?

1. Клиент отправляет действия на сервер
2. Сервер валидирует и применяет
3. Сервер отправляет обновления всем клиентам (60Hz)
4. Клиент предсказывает и интерполирует для плавности

---

**Версия**: 0.4.20421  
**Последнее обновление**: 2026-01-27

