# Список задач для исправления синхронизации мультиплеера

**Дата создания:** 2025-01-XX  
**Всего задач:** 23  
**Статус:** Активная разработка

---

## 📋 СПИСОК ЗАДАЧ

### 🔴 ПРИОРИТЕТ 1: Проверка и исправление критических проблем

#### 1. Проверить накопление ошибки квантования
**Статус:** ⏳ pending  
**Файлы:** `src/shared/protocol.ts:119`, `src/server/deltaCompression.ts:56`

**Задачи:**
- Добавить тест который отслеживает разницу между отправленной и полученной позицией за длительный период (5+ минут игры)
- Проверить дрифт позиций
- Реализовать периодическую отправку полных позиций без квантования каждые 60 пакетов (1 раз в секунду при 60Hz)
- Добавить флаг `isFullState` в `PlayerStatesData`
- Модифицировать `DeltaCompressor` для поддержки полных состояний
- Протестировать что дрифт уменьшился

---

#### 2. Проверить соответствие физики клиента и сервера
**Статус:** ⏳ pending  
**Файлы:** `src/client/tankController.ts`, `src/server/room.ts:464`

**Задачи:**
- Сравнить параметры движения на клиенте и сервере
- Проверить что `moveSpeed=20` и `turnSpeed=4.4` совпадают
- Проверить расхождения в движении при тестировании
- Синхронизировать параметры или использовать серверную физику как авторитетную
- Добавить константы для параметров движения в `shared/types.ts`

---

#### 3. Проверить порядок вызовов в основном цикле
**Статус:** ⏳ pending  
**Файлы:** `src/client/game.ts:6980`, `src/client/tankController.ts:1011`

**Задачи:**
- Найти где вызывается `updateMultiplayer()` в `game.ts`
- Убедиться что порядок правильный: физика → `onAfterPhysicsObservable` → `updatePositionCache()` → `updateMultiplayer()`
- Добавить логирование порядка вызовов с временными метками
- Исправить если порядок неправильный
- Протестировать что позиция берется после физики

---

#### 4. Исправить фиксированный deltaTime на сервере
**Статус:** ⏳ pending  
**Файлы:** `src/server/gameServer.ts:854`, `src/server/room.ts:460`

**Задачи:**
- Найти где используется `deltaTime=1/60` в `handlePlayerInput()` (gameServer.ts:854)
- Заменить на реальный `deltaTime` из game loop (`room.update(deltaTime)`)
- Использовать реальный `deltaTime` в `updatePlayerPosition()`
- Проверить что расхождения уменьшились
- Протестировать с различными FPS на клиенте

---

#### 5. Проверить использование velocity для dead reckoning
**Статус:** ⏳ pending  
**Файлы:** `src/server/player.ts:81`, `src/client/networkPlayerTank.ts:198`

**Задачи:**
- Проверить что `velocity` отправляется с сервера в `toPlayerData()` (player.ts:81)
- Проверить что `NetworkPlayerTank` использует `velocity` для экстраполяции при потере пакетов (networkPlayerTank.ts:198)
- Реализовать dead reckoning если не используется
- Протестировать что при потере пакетов игроки продолжают двигаться плавно

---

#### 6. Исправить синхронизацию высоты (Y координата)
**Статус:** ⏳ pending  
**Файлы:** `src/server/room.ts:477`

**Задачи:**
- Проверить что серверная симуляция учитывает высоту при движении по неровной местности (room.ts:477 - Y всегда 0)
- Добавить синхронизацию высоты отдельно или учитывать в симуляции движения
- Получить высоту террейна через `getGroundHeight()`
- Протестировать на неровной местности
- Проверить что танк не проваливается под землю

---

#### 7. Добавить синхронизацию turretRotation и aimPitch при reconciliation
**Статус:** ⏳ pending  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:709`

**Задачи:**
- Обновить `handleReconciliation()` (GameMultiplayerCallbacks.ts:709) чтобы синхронизировать `turretRotation` и `aimPitch` из `serverState`
- Обновить `turret.rotation.y` и `barrel.rotation.x` при hard/soft correction
- Добавить учет погрешности квантования для углов
- Протестировать синхронизацию башни при reconciliation

---

#### 8. Проверить что мировые координаты используются везде
**Статус:** ⏳ pending  
**Файлы:** `src/client/game.ts:6993, 7014`

**Задачи:**
- Убедиться что `getCachedChassisPosition()` используется во всех местах отправки позиций (game.ts:6993, 7014)
- Проверить что нет других мест где используется `chassis.position` для отправки
- Добавить проверку в линтер
- Протестировать что позиции синхронизированы

---

#### 9. Проверить синхронизацию physics body
**Статус:** ⏳ pending  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:713`

**Задачи:**
- Протестировать что после reconciliation танк не прыгает обратно
- Проверить что `computeWorldMatrix(true)` вызывается (GameMultiplayerCallbacks.ts:713)
- Проверить что `updatePositionCache()` вызывается после reconciliation
- Добавить визуальную проверку (танк должен оставаться на месте)

---

### 🟡 ПРИОРИТЕТ 2: Реализация улучшений

#### 10. Реализовать периодическую отправку полных состояний
**Статус:** ⏳ pending  
**Файлы:** `src/server/deltaCompression.ts:56`

**Задачи:**
- Добавить счетчик пакетов в `DeltaCompressor` (deltaCompression.ts:56)
- Каждые 60 пакетов отправлять полное состояние без дельта-компрессии
- Добавить флаг `isFullState` в `PlayerStatesData`
- Модифицировать `compressPlayerStates()` для поддержки полных состояний
- Убедиться что клиент обрабатывает полные состояния правильно
- Протестировать что накопление ошибок уменьшилось

---

#### 11. Улучшить обработку больших расхождений при reconciliation
**Статус:** ⏳ pending  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:699`

**Задачи:**
- Добавить обработку очень больших расхождений (>10 единиц) с логированием и диагностикой в `handleReconciliation()`
- Добавить предупреждение если расхождение >20 единиц (возможно проблема с сетью)
- Добавить метрику для отслеживания больших расхождений
- Возможно добавить автоматическую диагностику причин

---

#### 12. Оптимизировать избыточное логирование
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts`, `src/server/gameServer.ts`

**Задачи:**
- Уменьшить частоту диагностических логов (например, раз в 60 пакетов вместо каждого) в multiplayer.ts и gameServer.ts
- Сделать логирование условным через флаг `DEBUG_SYNC` в конфиге
- Убрать избыточные `console.log` из production кода
- Оставить только критические логи для диагностики

---

#### 13. Добавить метрики синхронизации
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts`, `src/client/debugDashboard.ts`

**Задачи:**
- Создать `SyncMetrics` класс для отслеживания средней разницы позиций, частоты reconciliation, количества больших расхождений
- Добавить отображение метрик в debug dashboard (F3)
- Экспорт метрик в JSON/CSV для анализа
- Добавить графики расхождений во времени

---

#### 14. Добавить визуализацию расхождений для отладки
**Статус:** ⏳ pending  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts`, `src/client/debugDashboard.ts`

**Задачи:**
- Нарисовать линию от предсказанной позиции к серверной позиции при reconciliation (использовать BABYLON.LinesMesh)
- Добавить визуализацию в debug dashboard (F3)
- Показывать разницу позиций числом над танком
- Добавить опцию включения/выключения в настройках отладки

---

### 🟢 ПРИОРИТЕТ 3: Тестирование и валидация

#### 15. Протестировать синхронизацию с двумя клиентами
**Статус:** ⏳ pending

**Задачи:**
- Запустить два клиента в одной комнате (FFA режим)
- Проверить что игроки видят друг друга на правильных позициях
- Проверить что локальный игрок видит себя на правильной позиции
- Проверить что движения синхронизированы
- Проверить reconciliation при движении
- Записать результаты тестирования
- Создать отчет о тестировании

---

#### 16. Проверить расчет позиции на сервере
**Статус:** ⏳ pending  
**Файлы:** `src/server/room.ts:460`

**Задачи:**
- Убедиться что `updatePlayerPosition()` (room.ts:460) использует правильные формулы движения
- Проверить что позиция рассчитывается идентично клиенту
- Добавить логирование для сравнения клиент-сервер позиций каждые 60 пакетов
- Проверить что формулы совпадают с клиентом

---

#### 17. Проверить что Spatial Partitioning не фильтрует локального игрока
**Статус:** ⏳ pending  
**Файлы:** `src/server/gameServer.ts:1529`

**Задачи:**
- Убедиться что локальный игрок всегда включен в `playersToSend` независимо от spatial grid (gameServer.ts:1529)
- Протестировать с большим количеством игроков (>10)
- Проверить что reconciliation работает даже при фильтрации других игроков
- Добавить логирование для диагностики

---

#### 18. Проверить что jitter buffer правильно обходит reconciliation
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts:1871`

**Задачи:**
- Убедиться что reconciliation для локального игрока обрабатывается немедленно (multiplayer.ts:1871)
- Проверить что `hasLocalPlayer` правильно определяет наличие локального игрока
- Протестировать с различными задержками сети (100ms, 200ms, 500ms)
- Проверить что reconciliation не задерживается

---

#### 19. Проверить правильность порога квантования
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts:2732`, `src/client/game/GameMultiplayerCallbacks.ts:688`

**Задачи:**
- Убедиться что `QUANTIZATION_ERROR=0.15` правильно учитывает погрешность 0.1 единицы (multiplayer.ts:2732, GameMultiplayerCallbacks.ts:688)
- Протестировать что reconciliation не срабатывает из-за квантования
- Возможно увеличить порог если нужно
- Добавить тесты для проверки

---

#### 20. Добавить валидацию истории позиций
**Статус:** ⏳ pending  
**Файлы:** `src/server/player.ts:127, 137`

**Задачи:**
- Проверить что `positionHistory` на сервере правильно обновляется (player.ts:127)
- Убедиться что `getPositionAtTime()` работает правильно для lag compensation (player.ts:137)
- Протестировать lag compensation при стрельбе
- Проверить что rewind time работает правильно

---

#### 21. Проверить что скорость интерполяции достаточна
**Статус:** ⏳ pending  
**Файлы:** `src/client/networkPlayerTank.ts:33, 442`

**Задачи:**
- Протестировать что другие игроки быстро следуют за сервером
- Проверить что `INTERPOLATION_SPEED=25` и множитель x3 работают правильно (networkPlayerTank.ts:33, 442)
- Возможно увеличить если игроки все еще отстают
- Протестировать с различными ping (50ms, 100ms, 200ms)

---

#### 22. Добавить тесты для reconciliation
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts:2702`

**Задачи:**
- Создать unit тесты для `reconcileServerState()` с различными сценариями (маленькие расхождения, большие расхождения, квантование)
- Протестировать что reconciliation правильно применяется
- Протестировать что physics body синхронизируется
- Добавить интеграционные тесты

---

#### 23. Проверить обновление сетевых игроков
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts:2185`

**Задачи:**
- Убедиться что `updateNetworkPlayer()` правильно обновляет позиции (multiplayer.ts:2185)
- Проверить что позиции обновляются при получении `PLAYER_STATES`
- Протестировать что интерполяция работает плавно
- Проверить что игроки не дёргаются

---

### 🔵 ПРИОРИТЕТ 4: Дополнительные улучшения

#### 24. Добавить индикатор качества синхронизации
**Статус:** ⏳ pending  
**Файлы:** `src/client/hud.ts`

**Задачи:**
- Показывать в HUD качество синхронизации (хорошо/плохо) на основе частоты reconciliation и расхождений
- Добавить цветовую индикацию (зеленый/желтый/красный)
- Показывать среднюю разницу позиций
- Добавить в настройки возможность включения/выключения

---

## 📊 Статистика задач

| Приоритет | Количество | Статус |
|-----------|------------|--------|
| 🔴 Приоритет 1 | 9 задач | ⏳ pending |
| 🟡 Приоритет 2 | 5 задач | ⏳ pending |
| 🟢 Приоритет 3 | 9 задач | ⏳ pending |
| 🔵 Приоритет 4 | 1 задача | ⏳ pending |
| **ИТОГО** | **24 задачи** | **⏳ pending** |

---

## 🎯 Рекомендуемый порядок выполнения

### Неделя 1: Критические проверки
1. Проверить порядок вызовов в основном цикле
2. Проверить что мировые координаты используются везде
3. Проверить синхронизацию physics body
4. Протестировать синхронизацию с двумя клиентами

### Неделя 2: Исправления и улучшения
5. Исправить фиксированный deltaTime на сервере
6. Добавить синхронизацию turretRotation и aimPitch
7. Исправить синхронизацию высоты
8. Реализовать периодическую отправку полных состояний

### Неделя 3: Оптимизация и тестирование
9. Проверить накопление ошибки квантования
10. Проверить соответствие физики клиента и сервера
11. Добавить метрики синхронизации
12. Оптимизировать логирование

### Неделя 4: Дополнительные улучшения
13. Добавить визуализацию расхождений
14. Добавить индикатор качества синхронизации
15. Добавить тесты для reconciliation
16. Финальное тестирование

---

**Последнее обновление:** 2025-01-XX

