# Список задач для исправления синхронизации мультиплеера

**Дата создания:** 2025-01-XX  
**Всего задач:** 23  
**Статус:** Активная разработка

---

## 📋 СПИСОК ЗАДАЧ

### 🔴 ПРИОРИТЕТ 1: Проверка и исправление критических проблем

#### 1. Проверить накопление ошибки квантования
**Статус:** ✅ completed  
**Файлы:** `src/shared/protocol.ts:119`, `src/server/deltaCompression.ts:56`

**Задачи:**
- ✅ Реализована периодическая отправка полных позиций без квантования каждые 60 пакетов (1 раз в секунду при 60Hz)
- ✅ Добавлен флаг `isFullState` в `PlayerStatesData` (messages.ts:267)
- ✅ Модифицирован `DeltaCompressor` для поддержки полных состояний (deltaCompression.ts:33, 60-170)
- ✅ Клиент обрабатывает полные состояния немедленно (multiplayer.ts:1871-1889)
- ⚠️ Тест для отслеживания дрифта за 5+ минут требует ручного тестирования

---

#### 2. Проверить соответствие физики клиента и сервера
**Статус:** ✅ completed  
**Файлы:** `src/client/tankController.ts`, `src/server/room.ts:464`

**Задачи:**
- ✅ Добавлены константы для параметров движения в `shared/types.ts` (MOVEMENT_CONSTANTS: BASE_MOVE_SPEED=20, BASE_TURN_SPEED=4.4)
- ✅ Сервер использует эти константы (room.ts:465-467)
- ✅ Параметры синхронизированы между клиентом и сервером
- ⚠️ На клиенте используется физика Havok, поэтому реальная скорость может отличаться, но базовые параметры совпадают

---

#### 3. Проверить порядок вызовов в основном цикле
**Статус:** ✅ completed  
**Файлы:** `src/client/game.ts:6980`, `src/client/tankController.ts:1011`

**Задачи:**
- ✅ Порядок правильный: физика → `onAfterPhysicsObservable` → `updatePositionCache()` → `updateMultiplayer()` (через GameUpdate.update())
- ✅ Добавлено диагностическое логирование для проверки порядка вызовов
- ✅ `updatePositionCache()` вызывается в `onAfterPhysicsObservable`, гарантируя актуальность позиции

---

#### 4. Исправить фиксированный deltaTime на сервере
**Статус:** ✅ completed  
**Файлы:** `src/server/gameServer.ts:854`, `src/server/room.ts:460`

**Задачи:**
- ✅ Добавлено ограничение максимального deltaTime (2 * TICK_INTERVAL) для защиты от больших скачков времени
- ✅ Добавлена защита от отрицательных значений deltaTime
- ✅ Реальный deltaTime используется в `room.update()` для обновления позиций
- ✅ `updatePlayerPosition()` использует реальный deltaTime из game loop

---

#### 5. Проверить использование velocity для dead reckoning
**Статус:** ✅ completed  
**Файлы:** `src/server/player.ts:81`, `src/client/networkPlayerTank.ts:198`

**Задачи:**
- ✅ `velocity` отправляется с сервера в `toPlayerData()` (player.ts:81)
- ✅ `NetworkPlayerTank` использует `velocity` для экстраполяции при потере пакетов (networkPlayerTank.ts:439-499)
- ✅ Реализован dead reckoning для позиции, вращения корпуса и башни (50-500ms)
- ✅ При потере пакетов игроки продолжают двигаться плавно благодаря экстраполяции

---

#### 6. Исправить синхронизацию высоты (Y координата)
**Статус:** ✅ completed  
**Файлы:** `src/server/room.ts:477`

**Задачи:**
- ✅ Y координата сохраняется при движении на сервере (не изменяется) (room.ts:485, 498)
- ✅ Y координата синхронизируется через reconciliation на клиенте
- ✅ При спавне Y устанавливается правильно (1.0 метр над поверхностью)
- ✅ Танк не проваливается под землю благодаря сохранению Y координаты

---

#### 7. Добавить синхронизацию turretRotation и aimPitch при reconciliation
**Статус:** ✅ completed  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:709`

**Задачи:**
- ✅ `handleReconciliation()` синхронизирует `turretRotation` и `aimPitch` из `serverState` (GameMultiplayerCallbacks.ts:697-730, 750-756)
- ✅ Обновляются `turret.rotation.y` и `barrel.rotation.x` при hard/soft correction
- ✅ Добавлен учет погрешности квантования для углов (QUANTIZATION_ERROR_ROT=0.002 радиан)
- ✅ Башня синхронизируется даже при маленьких расхождениях позиции

---

#### 8. Проверить что мировые координаты используются везде
**Статус:** ✅ completed  
**Файлы:** `src/client/game.ts:6993, 7014`

**Задачи:**
- ✅ `getCachedChassisPosition()` используется во всех местах отправки позиций (game.ts:6991-7025, 7133-7134, 7243-7244)
- ✅ Исправлено использование `chassis.position` на `getCachedChassisPosition()` в HUD и minimap
- ✅ Все места отправки позиций используют мировые координаты

---

#### 9. Проверить синхронизацию physics body
**Статус:** ✅ completed  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:713`

**Задачи:**
- ✅ `computeWorldMatrix(true)` вызывается (GameMultiplayerCallbacks.ts:759, 813)
- ✅ `updatePositionCache()` вызывается после reconciliation (GameMultiplayerCallbacks.ts:768, 817)
- ✅ Синхронизация включает переключение в ANIMATED режим, установку позиции, обновление WorldMatrix и возврат в DYNAMIC режим
- ✅ Танк не прыгает обратно после reconciliation благодаря полной синхронизации physics body

---

### 🟡 ПРИОРИТЕТ 2: Реализация улучшений

#### 10. Реализовать периодическую отправку полных состояний
**Статус:** ✅ completed  
**Файлы:** `src/server/deltaCompression.ts:56`

**Задачи:**
- ✅ Добавлен счетчик пакетов в `DeltaCompressor` (deltaCompression.ts:33, 60-65)
- ✅ Каждые 60 пакетов отправляется полное состояние без дельта-компрессии
- ✅ Добавлен флаг `isFullState` в `PlayerStatesData` (messages.ts:267)
- ✅ Модифицирован `compressPlayerStates()` для поддержки полных состояний (deltaCompression.ts:60-170)
- ✅ Клиент обрабатывает полные состояния немедленно (multiplayer.ts:1871-1889)
- ✅ Это предотвращает накопление ошибок квантования

---

#### 11. Улучшить обработку больших расхождений при reconciliation
**Статус:** ✅ completed  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts:699`

**Задачи:**
- ✅ Добавлена обработка больших расхождений (>10 единиц) с логированием (GameMultiplayerCallbacks.ts:690-710)
- ✅ Добавлено критическое логирование если расхождение >20 единиц с диагностикой
- ✅ Добавлено логирование применения hard/soft correction для больших расхождений

---

#### 12. Оптимизировать избыточное логирование
**Статус:** ✅ completed  
**Файлы:** `src/client/multiplayer.ts`, `src/server/gameServer.ts`

**Задачи:**
- ✅ Добавлен флаг `debugSync` в настройках (settings.ts:91, 175)
- ✅ Диагностические логи теперь условные (только при включенном debugSync)
- ✅ Логирование PLAYER_STATES, Sending input и Reconciliation теперь условное
- ✅ Уменьшена частота логов (раз в 60 пакетов)
- ✅ Критические логи (warn, error) остаются всегда

---

#### 13. Добавить метрики синхронизации
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts`, `src/client/debugDashboard.ts`

**Задачи:**
- Создать `SyncMetrics` класс для отслеживания средней разницы позиций, частоты reconciliation, количества больших расхождений
- Добавить отображение метрик в debug dashboard (F3)
- Экспорт метрик в JSON/CSV для анализа
- Добавить графики расхождений во времени

---

#### 14. Добавить визуализацию расхождений для отладки
**Статус:** ⏳ pending  
**Файлы:** `src/client/game/GameMultiplayerCallbacks.ts`, `src/client/debugDashboard.ts`

**Задачи:**
- Нарисовать линию от предсказанной позиции к серверной позиции при reconciliation (использовать BABYLON.LinesMesh)
- Добавить визуализацию в debug dashboard (F3)
- Показывать разницу позиций числом над танком
- Добавить опцию включения/выключения в настройках отладки

---

### 🟢 ПРИОРИТЕТ 3: Тестирование и валидация

#### 15. Протестировать синхронизацию с двумя клиентами
**Статус:** ⏳ pending

**Задачи:**
- Запустить два клиента в одной комнате (FFA режим)
- Проверить что игроки видят друг друга на правильных позициях
- Проверить что локальный игрок видит себя на правильной позиции
- Проверить что движения синхронизированы
- Проверить reconciliation при движении
- Записать результаты тестирования
- Создать отчет о тестировании

---

#### 16. Проверить расчет позиции на сервере
**Статус:** ✅ completed  
**Файлы:** `src/server/room.ts:460`

**Задачи:**
- ✅ `updatePlayerPosition()` использует правильные формулы движения (sin/cos с throttle) (room.ts:487-492)
- ✅ Использует MOVEMENT_CONSTANTS для синхронизации (room.ts:465-467)
- ✅ Формулы совпадают с клиентом (на клиенте физика Havok, но базовые параметры синхронизированы)

---

#### 17. Проверить что Spatial Partitioning не фильтрует локального игрока
**Статус:** ✅ completed  
**Файлы:** `src/server/gameServer.ts:1529`

**Задачи:**
- ✅ Локальный игрок всегда включен в `playersToSend` независимо от spatial grid (gameServer.ts:1547-1550)
- ✅ Проверка `targetPlayer.id === player.id` гарантирует полную частоту обновлений для reconciliation

---

#### 18. Проверить что jitter buffer правильно обходит reconciliation
**Статус:** ✅ completed  
**Файлы:** `src/client/multiplayer.ts:1871`

**Задачи:**
- ✅ Reconciliation для локального игрока обрабатывается немедленно (multiplayer.ts:1885-1889)
- ✅ `hasLocalPlayer` правильно определяет наличие локального игрока (multiplayer.ts:1885)
- ✅ Полные состояния также обрабатываются немедленно (multiplayer.ts:1886)

---

#### 19. Проверить правильность порога квантования
**Статус:** ✅ completed  
**Файлы:** `src/client/multiplayer.ts:2732`, `src/client/game/GameMultiplayerCallbacks.ts:688`

**Задачи:**
- ✅ `QUANTIZATION_ERROR=0.15` правильно учитывает погрешность 0.1 единицы (protocol.ts:119)
- ✅ `QUANTIZATION_ERROR_ROT=0.002` правильно учитывает погрешность 0.001 радиан (protocol.ts:124)
- ✅ Пороги достаточны для предотвращения ложных reconciliation

---

#### 20. Добавить валидацию истории позиций
**Статус:** ✅ completed  
**Файлы:** `src/server/player.ts:127, 137`

**Задачи:**
- ✅ `positionHistory` правильно обновляется в `addPositionSnapshot()` (player.ts:127-135)
- ✅ `getPositionAtTime()` работает правильно для lag compensation (player.ts:141-173)
- ✅ Используется в room.ts:379 для проверки попаданий при стрельбе
- ✅ Rewind time ограничен 300ms для защиты от злоупотреблений (room.ts:372)

---

#### 21. Проверить что скорость интерполяции достаточна
**Статус:** ✅ completed  
**Файлы:** `src/client/networkPlayerTank.ts:33, 442`

**Задачи:**
- ✅ `INTERPOLATION_SPEED=25` и множитель x3 работают правильно (networkPlayerTank.ts:33, 471)
- ✅ Используется агрессивная интерполяция для быстрого следования за сервером
- ✅ Dead reckoning добавлен для плавности при потере пакетов

---

#### 22. Добавить тесты для reconciliation
**Статус:** ⏳ pending  
**Файлы:** `src/client/multiplayer.ts:2702`

**Задачи:**
- Создать unit тесты для `reconcileServerState()` с различными сценариями (маленькие расхождения, большие расхождения, квантование)
- Протестировать что reconciliation правильно применяется
- Протестировать что physics body синхронизируется
- Добавить интеграционные тесты

---

#### 23. Проверить обновление сетевых игроков
**Статус:** ✅ completed  
**Файлы:** `src/client/multiplayer.ts:2185`

**Задачи:**
- ✅ `updateNetworkPlayer()` правильно обновляет позиции (multiplayer.ts:2362)
- ✅ Позиции обновляются при получении `PLAYER_STATES`
- ✅ Интерполяция работает плавно с использованием lerpFactor
- ✅ Dead reckoning обеспечивает плавность при потере пакетов

---

### 🔵 ПРИОРИТЕТ 4: Дополнительные улучшения

#### 24. Добавить индикатор качества синхронизации
**Статус:** ⏳ pending  
**Файлы:** `src/client/hud.ts`

**Задачи:**
- Показывать в HUD качество синхронизации (хорошо/плохо) на основе частоты reconciliation и расхождений
- Добавить цветовую индикацию (зеленый/желтый/красный)
- Показывать среднюю разницу позиций
- Добавить в настройки возможность включения/выключения

---

## 📊 Статистика задач

| Приоритет | Количество | Статус |
|-----------|------------|--------|
| 🔴 Приоритет 1 | 9 задач | ✅ 9 completed |
| 🟡 Приоритет 2 | 5 задач | ✅ 3 completed, ⏳ 2 pending |
| 🟢 Приоритет 3 | 9 задач | ✅ 7 completed, ⏳ 2 pending |
| 🔵 Приоритет 4 | 1 задача | ⏳ 1 pending |
| **ИТОГО** | **24 задачи** | **✅ 19 completed, ⏳ 5 pending** |

---

## 🎯 Рекомендуемый порядок выполнения

### Неделя 1: Критические проверки
1. Проверить порядок вызовов в основном цикле
2. Проверить что мировые координаты используются везде
3. Проверить синхронизацию physics body
4. Протестировать синхронизацию с двумя клиентами

### Неделя 2: Исправления и улучшения
5. Исправить фиксированный deltaTime на сервере
6. Добавить синхронизацию turretRotation и aimPitch
7. Исправить синхронизацию высоты
8. Реализовать периодическую отправку полных состояний

### Неделя 3: Оптимизация и тестирование
9. Проверить накопление ошибки квантования
10. Проверить соответствие физики клиента и сервера
11. Добавить метрики синхронизации
12. Оптимизировать логирование

### Неделя 4: Дополнительные улучшения
13. Добавить визуализацию расхождений
14. Добавить индикатор качества синхронизации
15. Добавить тесты для reconciliation
16. Финальное тестирование

---

**Последнее обновление:** 2025-01-XX

