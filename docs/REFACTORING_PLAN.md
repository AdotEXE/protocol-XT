# План реструктуризации больших файлов

## ✅ СТАТУС: ЗАВЕРШЕНО (2025-12-25)

Все основные задачи рефакторинга выполнены! Проект теперь имеет модульную архитектуру.

## Обзор

Проект содержал несколько очень больших файлов, которые были разделены на модули для улучшения поддерживаемости и читаемости кода.

## Начальное состояние

### Большие файлы (до рефакторинга):
1. **`src/client/tankController.ts`** - 9,836 строк
2. **`src/client/menu.ts`** - 5,146 строк
3. **`src/client/garage.ts`** - 3,973 строки

## ✅ Завершённые модули

### Tank модули (`src/client/tank/`):
- ✅ `tankChassis.ts` - создание корпусов (~3300 строк)
- ✅ `tankCannon.ts` - создание пушек (~1812 строк)
- ✅ `tankSpecialAbilities.ts` - анимации специальных способностей
- ✅ `movement/TankMovementConfig.ts` - конфигурация движения
- ✅ `combat/TankWeaponConfig.ts` - конфигурация оружия
- ✅ `combat/TankAiming.ts` - логика прицеливания
- ✅ `combat/TankDamage.ts` - расчёт урона

### Menu модули (`src/client/menu/`):
- ✅ `settings.ts` - работа с настройками игры
- ✅ `skillTreeUI.ts` - UI скил-дерева
- ✅ `screens/MainMenuScreen.ts` - главный экран меню
- ✅ `screens/SettingsScreen.ts` - экран настроек

### Garage модули (`src/client/garage/`):
- ✅ `ui.ts` - HTML/CSS UI логика
- ✅ `preview.ts` - 3D превью логика
- ✅ `materials.ts` - фабрика материалов
- ✅ `chassisDetails.ts` - генератор деталей корпусов
- ✅ `cannonDetails.ts` - генератор деталей пушек

### Game модули (`src/client/game/`):
- ✅ `LoadingScreen.ts` - экран загрузки
- ✅ `SettingsManager.ts` - управление настройками
- ✅ `FrontlineMode.ts` - режим Frontline

### HUD модули (`src/client/hud/components/`):
- ✅ `Crosshair.ts` - компонент прицела
- ✅ `HealthBar.ts` - компонент полосы здоровья
- ✅ `Minimap.ts` - компонент миникарты
- ✅ `Compass.ts` - компонент компаса
- ✅ `ConsumablesBar.ts` - компонент панели расходников

### Maps модули (`src/client/maps/`):
- ✅ `shared/BaseMapGenerator.ts` - базовый класс генераторов
- ✅ `shared/ChunkHelpers.ts` - вспомогательные методы
- ✅ 8 генераторов карт (Polygon, Frontline, Ruins, Canyon, Industrial, Urban, Underground, Coastal)

## Следующие шаги

### 1. Рефакторинг tankController.ts

#### Шаг 1.1: Вынести createUniqueChassis
```typescript
// В tankController.ts заменить:
private createUniqueChassis(...) { ... }

// На:
import { createUniqueChassis } from "./tank/tankChassis";
// Использовать функцию из модуля
```

**Что нужно сделать:**
1. Скопировать метод `createUniqueChassis` из `tankController.ts` в `tank/tankChassis.ts`
2. Адаптировать его для работы без `this` контекста
3. Заменить в `tankController.ts` вызов на импортированную функцию

#### Шаг 1.2: Вынести addChassisDetails
```typescript
// В tankController.ts заменить:
private addChassisDetails(...) { ... }

// На:
import { addChassisDetails } from "./tank/tankChassis";
// Использовать функцию из модуля
```

**Что нужно сделать:**
1. Скопировать метод `addChassisDetails` (очень большой switch по типам корпусов)
2. Убедиться, что все зависимости доступны (материалы, ChassisDetailsGenerator)
3. Адаптировать для работы без `this` контекста

#### Шаг 1.3: Вынести createUniqueCannon
```typescript
// В tankController.ts заменить:
private createUniqueCannon(...) { ... }

// На:
import { createUniqueCannon } from "./tank/tankCannon";
// Использовать функцию из модуля
```

**Что нужно сделать:**
1. Скопировать метод `createUniqueCannon` (большой switch по типам пушек)
2. Адаптировать для работы без `this` контекста

#### Шаг 1.4: Вынести анимации специальных способностей
```typescript
// В tankController.ts метод update() заменить вызовы анимаций:
import { updateSpecialAbilityAnimation } from "./tank/tankSpecialAbilities";

// В методе update():
updateSpecialAbilityAnimation(this.chassisAnimationElements, deltaTime);
```

**Что нужно сделать:**
1. Скопировать всю логику анимаций из `tankController.ts::update()`
2. Скопировать методы активации/деактивации способностей
3. Адаптировать для работы через параметры вместо `this`

### 2. Рефакторинг menu.ts

#### Шаг 2.1: Вынести работу с настройками
```typescript
// В menu.ts заменить:
const DEFAULT_SETTINGS = { ... };
function loadSettings() { ... }
function saveSettings() { ... }

// На:
import { loadSettings, saveSettings, DEFAULT_SETTINGS } from "./menu/settings";
```

**Что нужно сделать:**
1. Скопировать все функции работы с настройками
2. Скопировать интерфейс GameSettings
3. Убедиться, что applySettings правильно применяет настройки

#### Шаг 2.2: Вынести UI скил-дерева
```typescript
// В menu.ts заменить:
function createSkillTreeUI() { ... }
function updateSkillTreeDisplay() { ... }

// На:
import { createSkillTreeUI, updateSkillTreeDisplay, handleSkillClick } from "./menu/skillTreeUI";
```

**Что нужно сделать:**
1. Найти все методы, связанные со скил-деревом
2. Скопировать их в `skillTreeUI.ts`
3. Убедиться, что зависимости доступны

### 3. Рефакторинг garage.ts

#### Шаг 3.1: Вынести UI логику
```typescript
// В garage.ts заменить:
private createOverlay() { ... }
private renderItems() { ... }
private setupKeyboardNavigation() { ... }

// На:
import { createGarageOverlay, updateGarageItemsDisplay, setupGarageKeyboardNavigation } from "./garage/ui";
```

**Что нужно сделать:**
1. Скопировать HTML/CSS логику
2. Вынести состояние в интерфейс GarageUIState
3. Адаптировать для работы через параметры

#### Шаг 3.2: Вынести preview логику
```typescript
// В garage.ts заменить:
private initPreview() { ... }
private createUniqueChassisPreview() { ... }
private createUniqueCannonPreview() { ... }
private updatePreview() { ... }

// На:
import { initPreviewScene, createPreviewTank, updatePreviewTank } from "./garage/preview";
```

**Что нужно сделать:**
1. Скопировать всю логику 3D превью
2. Использовать уже существующие модули (ChassisDetailsGenerator, CannonDetailsGenerator)
3. Адаптировать для работы через параметры

## Важные замечания

1. **Постепенный рефакторинг**: Делать по одному модулю за раз, тестировать после каждого изменения

2. **Сохранять функциональность**: Убедиться, что после рефакторинга всё работает так же

3. **Тестирование**: После каждого шага проверить:
   - Компиляция без ошибок
   - Запуск игры
   - Функциональность (создание танков, меню, гараж)

4. **Зависимости**: Проверить все импорты и зависимости между модулями

5. **Контекст this**: При переносе методов из классов в функции, нужно передавать необходимые данные через параметры

## Оценка сложности

- **tankController.ts**: 🔴 Высокая сложность (очень большой файл, много зависимостей)
- **menu.ts**: 🟡 Средняя сложность (много UI логики, но хорошо структурирован)
- **garage.ts**: 🟢 Низкая сложность (уже частично рефакторен, есть модули)

## Рекомендации

1. Начать с `garage.ts` - самый простой случай
2. Затем `menu.ts` - средняя сложность
3. В конце `tankController.ts` - самый сложный, но самый важный

## Проверка после рефакторинга

После каждого этапа проверить:
- [ ] Код компилируется без ошибок
- [ ] Нет ошибок TypeScript
- [ ] Игра запускается
- [ ] Основная функциональность работает
- [ ] Нет регрессий



