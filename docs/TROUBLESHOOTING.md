# Руководство по устранению неполадок

## 🔍 Быстрая диагностика

### Проверка консоли браузера
1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку "Console"
3. Проверьте наличие ошибок (красные сообщения)
4. Проверьте предупреждения (желтые сообщения)

### Проверка производительности
1. Откройте консоль разработчика (F12)
2. Перейдите на вкладку "Performance"
3. Запишите сессию игры
4. Проверьте FPS и использование памяти

## 🚨 Проблемы с запуском

### Черный экран при запуске игры

**Симптомы**: Экран остается черным после нажатия "Start Game"

**Решение**: 
✅ Эта проблема была исправлена в последней версии. Если проблема сохраняется:

1. **Очистите кэш браузера**:
   - Chrome/Edge: Ctrl+Shift+Delete → Очистить кэш
   - Firefox: Ctrl+Shift+Delete → Кэш

2. **Проверьте консоль на ошибки**:
   - Откройте F12 → Console
   - Ищите ошибки связанные с камерой или сценой

3. **Проверьте версию браузера**:
   - Chrome 113+ или Edge 113+ (для WebGPU)
   - Или Firefox Nightly

4. **Перезагрузите страницу**: Ctrl+F5 (жесткая перезагрузка)

**Если проблема сохраняется**: Создайте issue с логами из консоли

### Ошибка "No camera defined"

**Симптомы**: Ошибка в консоли: `No camera defined`

**Решение**: ✅ Исправлено. Камера теперь создается автоматически при необходимости.

Если проблема сохраняется:
1. Проверьте, что используется последняя версия
2. Очистите кэш браузера
3. Перезапустите dev-сервер

### Ошибка "Cannot read properties of undefined"

**Симптомы**: Ошибка при инициализации игры

**Решение**: ✅ Исправлено. Все подписки на события теперь происходят после инициализации.

Если проблема сохраняется:
1. Убедитесь, что все зависимости установлены: `npm install`
2. Проверьте версию Node.js: `node --version` (должна быть 20+)
3. Пересоберите проект: `npm run build`

## 🎮 Проблемы с физикой

### Танк трясется при движении

**Симптомы**: Танк визуально "дрожит" или создается эффект "нескольких танков"

**Решение**: ✅ Исправлено в последней версии. Проблема была связана с рассинхронизацией между физическим телом и визуальным мешем.

Если проблема сохраняется:
1. **Проверьте настройки физики** (F3):
   - `hoverStiffness` - уменьшите если танк "прыгает"
   - `hoverDamping` - увеличьте для более плавного движения
   - `movementDamping` - увеличьте для уменьшения тряски

2. **Проверьте FPS**:
   - Низкий FPS может вызывать визуальные артефакты
   - Закройте другие вкладки браузера
   - Уменьшите настройки графики

3. **Обновите браузер** до последней версии

### Танк "проваливается" сквозь землю

**Симптомы**: Танк падает под землю или проходит сквозь объекты

**Решение**:
1. **Проверьте настройки физики** (F3):
   - `hoverHeight` - должна быть положительной (например, 2.0)
   - `hoverStiffness` - увеличьте для более жесткой подвески
   - `hoverDamping` - увеличьте для стабильности

2. **Проверьте массу танка**:
   - `mass` - должна быть разумной (например, 1000-5000)

3. **Перезапустите игру** - иногда помогает сброс физики

### Танк слишком нестабилен

**Симптомы**: Танк легко опрокидывается или "прыгает"

**Решение**:
1. **Откройте Physics Manager** (F4)
2. **Увеличьте параметры стабильности**:
   - `uprightForce` - увеличьте (например, до 5000)
   - `uprightDamp` - увеличьте (например, до 100)
   - `movementDamping` - увеличьте (например, до 0.8)

3. **Уменьшите чувствительность**:
   - `hoverStiffness` - уменьшите если танк "прыгает"
   - `turnSpeed` - уменьшите для более плавных поворотов

4. **Проверьте центр масс**:
   - Центр масс должен быть низко (например, -0.4 по Y)

### Танк не движется

**Симптомы**: Танк не реагирует на клавиши W/A/S/D

**Решение**:
1. **Проверьте фокус браузера** - кликните на canvas
2. **Проверьте консоль** на наличие ошибок
3. **Проверьте настройки управления** в меню
4. **Попробуйте перезапустить игру**

## 🎥 Проблемы с камерой

### Камера не следует за танком

**Симптомы**: Камера остается на месте или движется неправильно

**Решение**:
1. **Нажмите C** для центрирования камеры
2. **Проверьте, что камера не заблокирована**
3. **Перезапустите игру** если проблема сохраняется

### Камера трясется

**Симптомы**: Камера дрожит при движении танка

**Решение**: ✅ Исправлено. Камера теперь обновляется после обновления физики.

Если проблема сохраняется:
1. **Проверьте FPS** - низкий FPS может вызывать тряску
2. **Закройте Dev Dashboard** (F3) если открыт
3. **Уменьшите настройки графики**

### Башня не поворачивается

**Симптомы**: Башня не реагирует на движение мыши

**Решение**:
1. **Проверьте, что используется мышь** (не клавиатура)
2. **Нажмите Z/X** для клавиатурного управления башней
3. **Проверьте настройки чувствительности мыши** в меню
4. **Проверьте фокус браузера** - кликните на canvas

## ⚡ Проблемы с производительностью

### Низкий FPS

**Симптомы**: Игра работает медленно, лагает

**Решение**:
1. **Закройте Debug Dashboard** (F2) если открыт
2. **Уменьшите renderDistance** в настройках
3. **Закройте другие вкладки браузера**
4. **Проверьте использование памяти**:
   - Откройте Task Manager (Ctrl+Shift+Esc)
   - Закройте другие приложения

5. **Обновите драйверы видеокарты**
6. **Используйте WebGPU** вместо WebGL2:
   - Проверьте в консоли, какой рендерер используется
   - Chrome 113+ поддерживает WebGPU

### Лаги при движении

**Симптомы**: Игра "зависает" или "тормозит" при движении

**Решение**:
1. **Проверьте консоль** на наличие ошибок
2. **Убедитесь, что используется последняя версия**
3. **Попробуйте уменьшить настройки графики**
4. **Проверьте, что Chunk System работает правильно**:
   - Откройте Dev Dashboard (F3)
   - Проверьте количество активных чанков

### Высокое использование памяти

**Симптомы**: Браузер использует много памяти

**Решение**:
1. **Проверьте количество врагов** на карте
2. **Уменьшите renderDistance**
3. **Перезапустите браузер** периодически
4. **Проверьте на утечки памяти** в консоли

## 🎨 Проблемы с графикой

### Объекты не отображаются

**Симптомы**: Некоторые объекты невидимы

**Решение**:
1. **Проверьте настройки видимости** в Dev Dashboard (F3)
2. **Проверьте, что объекты не за пределами renderDistance**
3. **Перезапустите игру**

### Текстуры не загружаются

**Симптомы**: Объекты отображаются без текстур

**Решение**:
1. **Проверьте консоль** на ошибки загрузки
2. **Очистите кэш браузера**
3. **Проверьте, что файлы в папке public/**

## 🔧 Проблемы с настройками

### Настройки не сохраняются

**Симптомы**: Настройки сбрасываются после перезапуска

**Решение**:
1. **Проверьте, что localStorage доступен** в браузере
2. **Отключите режим инкогнито** (настройки не сохраняются)
3. **Проверьте настройки приватности браузера**

### Горячие клавиши не работают

**Симптомы**: F2, F3, F4, F5, F6, F7 не работают

**Решение**: ✅ Исправлено в последней версии. Все F-клавиши теперь работают корректно.

Если проблема сохраняется:
1. **Проверьте, что игра запущена** (не в меню)
2. **Проверьте фокус браузера** - кликните на canvas
3. **Попробуйте перезапустить игру**
4. **Проверьте консоль** на ошибки

### Physics Manager не открывается

**Симптомы**: F4 не работает

**Решение**: ✅ Исправлено. F4 теперь работает корректно.

Если проблема сохраняется:
1. **Проверьте, что игра запущена** (не в меню)
2. **Проверьте, что панель инициализирована** - должно быть сообщение в консоли
3. **Попробуйте перезапустить игру**
4. **Проверьте консоль** на ошибки

### Dev Console не открывается

**Симптомы**: F5 не работает

**Решение**: ✅ Исправлено. F5 теперь работает корректно.

Если проблема сохраняется:
1. **Проверьте, что игра запущена** (не в меню)
2. **Проверьте, что ChatSystem инициализирован**
3. **Попробуйте перезапустить игру**
4. **Проверьте консоль** на ошибки

### Session Settings не открывается

**Симптомы**: F6 не работает

**Решение**: ✅ Исправлено. F6 теперь работает корректно.

Если проблема сохраняется:
1. **Проверьте, что игра запущена** (не в меню)
2. **Проверьте, что sessionSettings инициализирован**
3. **Попробуйте перезапустить игру**

### Cheat Menu не открывается

**Симптомы**: F7 не работает

**Решение**: ✅ Исправлено. F7 теперь работает корректно.

Если проблема сохраняется:
1. **Проверьте, что игра запущена** (не в меню)
2. **Проверьте, что cheatMenu инициализирован**
3. **Попробуйте перезапустить игру**

### Окно респавна не пропадает

**Симптомы**: После респавна окно смерти остается на экране

**Решение**: ✅ Исправлено. Окно теперь автоматически скрывается после респавна.

Если проблема сохраняется:
1. **Проверьте, что используется последняя версия**
2. **Очистите кэш браузера**
3. **Перезапустите игру**

### Ошибки Sentry в консоли

**Симптомы**: Ошибки `net::ERR_BLOCKED_BY_CLIENT` от sentry.io в консоли

**Решение**: ✅ Исправлено. Ошибки от внешних скриптов (расширения браузера, Sentry) теперь автоматически подавляются.

Эти ошибки не критичны и не влияют на работу игры. Они возникают из-за расширений браузера, которые пытаются отправить данные в Sentry, но блокируются блокировщиками рекламы.

## 🌐 Проблемы с сетью

### Не могу подключиться к серверу

**Симптомы**: Ошибки подключения к WebSocket серверу, сообщения об ошибках в консоли

**Решение**: 
1. **Убедитесь, что сервер запущен**: Выполните `npm run server` в отдельном терминале
2. **Проверьте адрес сервера**: По умолчанию используется `ws://localhost:8080`
3. **Проверьте настройки**: Можно указать другой адрес через переменную окружения `VITE_WS_SERVER_URL`
4. **Проверьте брандмауэр**: Убедитесь, что порт 8080 не заблокирован

**Статус мультиплеера**: Реализовано на 85-90%. Основная инфраструктура работает, но требуется тестирование и полировка.

## 📞 Получение помощи

### Перед созданием issue

1. ✅ Проверьте этот документ
2. ✅ Проверьте [CHANGELOG.md](../CHANGELOG.md) на известные проблемы
3. ✅ Проверьте [ARCHITECTURE.md](ARCHITECTURE.md) для понимания системы
4. ✅ Попробуйте воспроизвести проблему в последней версии

### Создание issue

При создании issue укажите:

1. **Описание проблемы** - что происходит
2. **Шаги для воспроизведения** - как воспроизвести
3. **Ожидаемое поведение** - что должно происходить
4. **Фактическое поведение** - что происходит
5. **Скриншоты/видео** - если применимо
6. **Окружение**:
   - Браузер и версия
   - ОС
   - Версия Node.js
7. **Логи консоли** - ошибки из консоли (F12)

### Полезные команды

```bash
# Проверка версии Node.js
node --version

# Переустановка зависимостей
rm -rf node_modules package-lock.json
npm install

# Очистка сборки
rm -rf dist
npm run build

# Проверка на ошибки TypeScript
npx tsc --noEmit
```

## ✅ Чеклист для диагностики

- [ ] Браузер обновлен до последней версии
- [ ] Node.js версия 20+ (LTS)
- [ ] Все зависимости установлены (`npm install`)
- [ ] Кэш браузера очищен
- [ ] Консоль проверена на ошибки
- [ ] FPS проверен (должен быть 60+)
- [ ] Используется последняя версия кода
- [ ] Проблема воспроизводится в других браузерах

## 🎯 Часто задаваемые вопросы (FAQ)

### Q: Почему игра не запускается?
**A**: Проверьте консоль браузера (F12) на наличие ошибок. Убедитесь, что используется Node.js 20+ и все зависимости установлены.

### Q: Почему танк трясется?
**A**: Эта проблема была исправлена. Если проблема сохраняется, проверьте настройки физики (F3) и FPS.

### Q: Как улучшить производительность?
**A**: Закройте Dev Dashboard (F3), уменьшите renderDistance, закройте другие вкладки браузера.

### Q: Где найти настройки?
**A**: Нажмите Escape для открытия меню, или используйте горячие клавиши:
- **F3** - Debug Dashboard
- **F4** - Physics Panel
- **F5** - System Terminal
- **F6** - Session Settings
- **F7** - Cheat Menu

### Q: Как включить игровой курсор?
**A**: Зажмите клавишу **Alt** во время игры. Курсор автоматически заблокируется (pointer lock). Отпустите Alt для разблокировки.

### Q: Почему окно респавна не пропадает?
**A**: Эта проблема была исправлена. Окно теперь автоматически скрывается после респавна. Если проблема сохраняется, очистите кэш браузера и перезапустите игру.

### Q: Где находятся слоты активных эффектов?
**A**: Слоты активных эффектов расположены справа от модулей (слоты 6-0). Они отображаются с плавным затуханием прозрачности слева направо.

### Q: Где находится блок состояния танка?
**A**: Блок состояния танка расположен слева от радара (внизу справа). Он показывает здоровье в процентах, топливо и броню.

### Q: Как сбросить настройки?
**A**: Очистите localStorage браузера или используйте функцию сброса в меню.

---

**Нужна помощь?** Создайте [issue](https://github.com/AdotEXE/protocol-XT/issues) с подробным описанием проблемы.
