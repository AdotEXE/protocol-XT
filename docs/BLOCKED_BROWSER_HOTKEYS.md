# 🚫 Блокируемые горячие клавиши браузера

## Обзор

Проект блокирует стандартные горячие клавиши браузера во время активной игры, чтобы предотвратить случайное закрытие вкладки, обновление страницы или другие нежелательные действия браузера.

**Файл:** `src/client/hotkeyManager.ts`

**Условие блокировки:** Блокировка активна только когда `isGameActive === true` и `blockBrowserHotkeys === true` (по умолчанию включено).

## 📋 Полный список блокируемых клавиш

### 🔄 Закрытие/обновление
- **Ctrl+W** - Закрыть вкладку
- **Ctrl+R** - Обновить страницу
- **F5** - Обновить страницу
- **F11** - Полноэкранный режим (перехватывается, но не полностью блокируется)

### 🧭 Навигация
- **Ctrl+D** - Добавить в закладки
- **Ctrl+G** - Поиск
- **Ctrl+H** - История браузера
- **Ctrl+J** - Загрузки
- **Ctrl+K** - Поиск (альтернативный)
- **Ctrl+L** - Адресная строка
- **Ctrl+N** - Новое окно
- **Ctrl+O** - Открыть файл
- **Ctrl+P** - Печать
- **Ctrl+S** - Сохранить
- **Ctrl+T** - Новая вкладка
- **Ctrl+U** - Просмотр исходного кода

### 🔍 Масштаб
- **Ctrl++** (Ctrl+Plus) - Увеличить масштаб
- **Ctrl+-** (Ctrl+Minus) - Уменьшить масштаб
- **Ctrl+0** - Сбросить масштаб

### 🔎 Другие
- **Ctrl+F** - Поиск на странице
- **Escape** - Обрабатывается игрой (закрытие чата, меню)

### ⌨️ Alt комбинации
- **Alt+←** (Alt+Left Arrow) - Назад в истории
- **Alt+→** (Alt+Right Arrow) - Вперед в истории
- **Alt+Home** - Домашняя страница
- **Alt+F4** - Закрыть окно (не блокируется браузером, но перехватывается)

## 🛡️ Дополнительная защита

### Контекстное меню
- **Правая кнопка мыши** - Блокируется во время игры (`contextmenu` событие)

### Предотвращение закрытия страницы
- **beforeunload** - Показывает диалог подтверждения при попытке закрыть вкладку:
  - Сообщение: "Вы уверены, что хотите покинуть игру?"

## ⚙️ Настройки

Блокировка управляется через `HotkeyManager`:

```typescript
// Включить/выключить блокировку
const hotkeyManager = getHotkeyManager();
hotkeyManager.setConfig({ blockBrowserHotkeys: false }); // Выключить
hotkeyManager.setConfig({ blockBrowserHotkeys: true });  // Включить
```

Настройки сохраняются в `localStorage` под ключом `hotkey-config`.

## 📝 Исключения

Блокировка **НЕ применяется**, если:
1. Фокус находится на `<input>`, `<textarea>` или элементе с `contenteditable="true"`
2. Игра не активна (`isGameActive === false`)
3. Блокировка отключена в настройках (`blockBrowserHotkeys === false`)

**Исключение:** `Escape` обрабатывается всегда, если открыт чат.

## 🔍 Логирование

При блокировке клавиши в консоль выводится сообщение:
```
[HotkeyManager] Blocked browser hotkey: Ctrl+W
```

## 📍 Расположение кода

- **Основной файл:** `src/client/hotkeyManager.ts`
- **Константа со списком:** `BLOCKED_BROWSER_HOTKEYS` (строки 29-64)
- **Метод проверки:** `shouldBlockKey()` (строки 191-204)
- **Обработчик:** `handleKeyDown()` (строки 130-173)

## 🎮 Игровые горячие клавиши (не блокируются)

Следующие клавиши используются игрой и **не блокируются**:

- **F2** - Скриншот
- **F7** - Unified Menu
- **F8** - Physics Editor
- **F9** - Network Menu
- **Enter** - Открыть чат
- **/** - Открыть чат с командой
- **V** - Push-to-talk (голосовой чат)
- **Escape** - Закрыть чат/меню
- **Tab** - Показать статистику
- **G** - Гараж
- **~** (Backquote) - Терминал

## ⚠️ Примечания

1. **F11 (полноэкранный режим)** - Перехватывается, но не полностью блокируется, так как это системная функция браузера.

2. **Alt+F4** - Не может быть полностью заблокирован в браузере, так как это системная комбинация Windows/Linux.

3. **Ctrl+Shift+I/J/C** - Блокируются в `main.ts` для предотвращения открытия DevTools (отдельная защита).

4. **F12** - Блокируется в `main.ts` для предотвращения открытия DevTools.

