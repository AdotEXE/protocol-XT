# 🚀 Деплой игрового сервера (Railway)

Vercel отлично подходит для фронтенда, но **не поддерживает WebSocket сервера** (Colyseus). Для мультиплеера нам нужен отдельный сервер.

Мы будем использовать **Railway** (есть бесплатный trial и отличная поддержка WebSocket).

## 📋 Инструкция

### 1. Подготовка
Убедитесь, что вы закоммитили последние изменения в git:
- `railway.json`
- Обновленный `package.json` (скрипты `build:server`, `start:server`)

### 2. Создание проекта в Railway

1. Зайдите на [railway.app](https://railway.app/)
2. Войдите через GitHub
3. Нажмите **+ New Project** -> **Deploy from GitHub repo**
4. Выберите ваш репозиторий `protocol-XT`
5. Railway автоматически обнаружит `railway.json` и начнет сборку

### 3. Настройка переменных окружения

В панели Railway перейдите в **Variables** и добавьте:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `PORT` | `8000` | Порт для WebSocket сервера |
| `HTTP_PORT` | `7000` | Порт для HTTP мониторинга |
| `NODE_ENV` | `production` | Режим работы |

### 4. Получение URL сервера

1. Перейдите в **Settings** -> **Networking**
2. Нажмите **Generate Domain**
3. Вы получите публичный URL, например: `protocol-xt-production.up.railway.app`

### 5. Подключение фронтенда к серверу

Теперь нужно сказать фронтенду (на Vercel), куда подключаться.

1. Зайдите в **Vercel** -> Ваш проект -> **Settings** -> **Environment Variables**
2. Добавьте переменную:
   - **Name:** `VITE_WS_SERVER_URL`
   - **Value:** `wss://protocol-xt-production.up.railway.app` (замените на ваш URL из Railway, **важно добавить wss://**)
3. **Передеплойте фронтенд** (Redeploy) в Vercel.

## ✅ Как это работает

1. Игрок заходит на сайт `protocol-xt.vercel.app` (Vercel)
2. Клиент загружается и читает `VITE_WS_SERVER_URL`
3. Клиент подключается по WebSocket к серверу на Railway (`wss://...`)
4. Игра работает! 🎮
