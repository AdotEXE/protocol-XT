# Руководство по деплою Protocol TX

## 🚀 Быстрый деплой (5 минут)

### Вариант 1: Vercel (Рекомендуется) ⭐

**Преимущества**: Бесплатно, автоматический деплой из GitHub, CDN по всему миру

**💰 Стоимость**:
- **Vercel Token** - **БЕСПЛАТНО** (это просто ключ доступа к API)
- **Hobby Plan (Бесплатный)** - **БЕСПЛАТНО** для личных проектов:
  - Неограниченное количество проектов
  - 100 GB bandwidth в месяц
  - Автоматические деплои из GitHub
  - CDN по всему миру
  - SSL сертификаты
  - **Этого более чем достаточно для вашей игры!**
- **Pro Plan** - $20/месяц (для коммерческих проектов)
- **Enterprise** - индивидуальные цены (для крупных компаний)

**Для вашего проекта достаточно бесплатного Hobby плана!** 🎉

#### Шаги:

1. **Соберите проект**:
```bash
npm run build
```

2. **Установите Vercel CLI** (опционально):
```bash
npm install -g vercel
```

3. **Деплой через веб-интерфейс**:
   - Зайдите на https://vercel.com
   - Войдите через GitHub
   - Нажмите "New Project"
   - Выберите репозиторий `protocol-XT`
   - Настройки:
     - **Framework Preset**: Vite
     - **Root Directory**: `./` (по умолчанию)
     - **Build Command**: `npm run build`
     - **Output Directory**: `dist`
   - Нажмите "Deploy"

4. **Или через CLI**:
```bash
vercel
```

**Готово!** Ваша игра будет доступна по адресу: `https://your-project.vercel.app`

---

### Вариант 2: Netlify

**Преимущества**: Бесплатно, простой деплой, хорошая производительность

#### Шаги:

1. **Соберите проект**:
```bash
npm run build
```

2. **Деплой через веб-интерфейс**:
   - Зайдите на https://app.netlify.com
   - Войдите через GitHub
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите репозиторий `protocol-XT`
   - Настройки:
     - **Build command**: `npm run build`
     - **Publish directory**: `dist`
   - Нажмите "Deploy site"

3. **Или через CLI**:
```bash
npm install -g netlify-cli
netlify deploy --prod
```

**Готово!** Ваша игра будет доступна по адресу: `https://your-project.netlify.app`

---

### Вариант 3: GitHub Pages

**Преимущества**: Бесплатно, интеграция с GitHub

#### Шаги:

1. **Установите gh-pages**:
```bash
npm install --save-dev gh-pages
```

2. **Добавьте скрипт в package.json**:
```json
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

3. **Настройте base в vite.config.ts**:
```typescript
export default defineConfig({
  base: '/protocol-XT/', // Имя вашего репозитория
  // ... остальная конфигурация
});
```

4. **Деплой**:
```bash
npm run deploy
```

5. **Включите GitHub Pages**:
   - Зайдите в Settings → Pages
   - Source: `gh-pages` branch
   - Save

**Готово!** Ваша игра будет доступна по адресу: `https://AdotEXE.github.io/protocol-XT`

---

### Вариант 4: Firebase Hosting

**Преимущества**: Интеграция с Firebase, хорошая производительность

#### Шаги:

1. **Установите Firebase CLI**:
```bash
npm install -g firebase-tools
```

2. **Войдите в Firebase**:
```bash
firebase login
```

3. **Инициализируйте проект**:
```bash
firebase init hosting
```
   - Выберите существующий проект или создайте новый
   - **Public directory**: `dist`
   - **Single-page app**: `Yes`
   - **Overwrite index.html**: `No`

4. **Соберите проект**:
```bash
npm run build
```

5. **Деплой**:
```bash
firebase deploy --only hosting
```

**Готово!** Ваша игра будет доступна по адресу: `https://your-project.web.app`

---

## 🔧 Настройка для продакшена

### 1. Проверка сборки

```bash
# Соберите проект
npm run build

# Проверьте, что dist/ содержит все файлы
ls dist/

# Должны быть:
# - index.html
# - assets/ (JS, CSS файлы)
# - HavokPhysics.wasm
```

### 2. Настройка base URL

Если деплоите в подпапку (например, GitHub Pages), обновите `vite.config.ts`:

```typescript
export default defineConfig({
  base: '/your-repo-name/', // Для GitHub Pages
  // или
  base: '/', // Для корневого домена
});
```

### 3. Проверка WASM файлов

Убедитесь, что `HavokPhysics.wasm` копируется в `dist/`:

```typescript
// vite.config.ts
export default defineConfig({
  publicDir: 'public', // Убедитесь, что public/ содержит HavokPhysics.wasm
});
```

### 4. Тестирование локально

```bash
# Соберите проект
npm run build

# Запустите preview
npm run preview

# Откройте http://localhost:4173
# Проверьте, что все работает
```

---

## 🌍 Настройка CDN и производительности

### Vercel/Netlify

Автоматически используют CDN. Никаких дополнительных настроек не требуется.

### GitHub Pages

Использует CDN GitHub. Может быть медленнее для некоторых регионов.

### Firebase Hosting

Использует CDN Google. Отличная производительность по всему миру.

---

## 🔒 Настройка домена (опционально)

### Vercel

1. Зайдите в Settings → Domains
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### Netlify

1. Зайдите в Site settings → Domain management
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

### Firebase

1. Зайдите в Hosting → Add custom domain
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

---

## 📊 Мониторинг и аналитика

### Добавление аналитики

1. **Google Analytics**:
```html
<!-- В index.html -->
<script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'GA_MEASUREMENT_ID');
</script>
```

2. **Vercel Analytics** (если используете Vercel):
   - Автоматически включена в Vercel Pro
   - Или установите `@vercel/analytics`

---

## 🐛 Решение проблем

### Проблема: GitHub Actions деплой падает с ошибкой "Input required and not supplied: vercel-token"

**Симптомы**: 
- Workflow выполняется, но шаг "Deploy to Vercel" падает
- Ошибка: `Error: Input required and not supplied: vercel-token`

**Решение**:
1. **Проверьте, что секреты добавлены в GitHub**:
   - Зайдите в **Settings** → **Secrets and variables** → **Actions**
   - Убедитесь, что есть три секрета: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`
   
2. **Проверьте правильность имен секретов**:
   - Имена должны быть **точно**: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`
   - Регистр важен! (все заглавные)
   
3. **Проверьте значения секретов**:
   - `VERCEL_TOKEN` должен быть действительным токеном из https://vercel.com/account/tokens
   - `VERCEL_ORG_ID` должен быть Team ID или Org ID из настроек проекта
   - `VERCEL_PROJECT_ID` должен быть Project ID из настроек проекта
   
4. **Перезапустите workflow**:
   - Зайдите в **Actions** → выберите failed workflow
   - Нажмите **Re-run all jobs**

**Альтернатива**: Если не хотите использовать автоматический деплой, можно деплоить вручную через веб-интерфейс Vercel (см. раздел "Вариант 1: Vercel" выше).

---

### Проблема: WASM файлы не загружаются

**Решение**:
1. Убедитесь, что `HavokPhysics.wasm` в папке `public/`
2. Проверьте, что файл копируется в `dist/`
3. Проверьте консоль браузера на ошибки CORS

### Проблема: Белый экран после деплоя

**Решение**:
1. Проверьте консоль браузера (F12)
2. Убедитесь, что `base` в `vite.config.ts` правильный
3. Проверьте, что все пути к файлам относительные

### Проблема: Медленная загрузка

**Решение**:
1. Включите сжатие (gzip) на хостинге
2. Используйте CDN (Vercel/Netlify делают это автоматически)
3. Оптимизируйте размер бандла

---

## ✅ Чеклист перед деплоем

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Preview работает локально (`npm run preview`)
- [ ] Все файлы в `dist/` на месте
- [ ] `HavokPhysics.wasm` копируется в `dist/`
- [ ] `base` в `vite.config.ts` настроен правильно
- [ ] Нет ошибок в консоли браузера
- [ ] Игра работает в разных браузерах
- [ ] Проверена производительность

---

## 🚀 Автоматический деплой (CI/CD)

### GitHub Actions для автоматического деплоя на Vercel

Проект уже содержит готовый workflow `.github/workflows/deploy-vercel.yml`, который автоматически деплоит проект при каждом push в ветку `main`.

#### ⚙️ Настройка секретов в GitHub

**ВАЖНО**: Перед использованием автоматического деплоя необходимо настроить секреты в GitHub:

1. **Получите Vercel Token** (бесплатно):
   - Зайдите на https://vercel.com/account/tokens
   - Нажмите "Create Token"
   - Скопируйте токен (он показывается только один раз!)
   - **💡 Токен бесплатный и работает даже на бесплатном Hobby плане**

2. **Получите Vercel Project ID и Org ID**:
   - Зайдите в ваш проект на Vercel
   - Откройте Settings → General
   - Найдите **Project ID** (например: `prj_xxxxxxxxxxxxx`)
   - Найдите **Team ID** или **Org ID** (например: `team_xxxxxxxxxxxxx`)

3. **Добавьте секреты в GitHub**:
   - Зайдите в ваш репозиторий на GitHub
   - Откройте **Settings** → **Secrets and variables** → **Actions**
   - Нажмите **New repository secret**
   - Добавьте три секрета:
     - **Имя**: `VERCEL_TOKEN` → **Значение**: ваш Vercel токен
     - **Имя**: `VERCEL_ORG_ID` → **Значение**: ваш Team/Org ID
     - **Имя**: `VERCEL_PROJECT_ID` → **Значение**: ваш Project ID

4. **Проверьте workflow**:
   - После добавления секретов, следующий push в `main` автоматически запустит деплой
   - Проверьте статус в **Actions** вкладке на GitHub

#### 📋 Структура workflow

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
```

#### ⚠️ Если секреты не настроены

Если секреты не настроены, workflow все равно выполнит сборку проекта, но пропустит деплой с информативным сообщением. Это позволяет проверять, что проект собирается без ошибок, даже без настроенного деплоя.

#### 🔍 Проверка статуса деплоя

1. Зайдите в **Actions** вкладку на GitHub
2. Выберите последний workflow run
3. Проверьте логи каждого шага
4. Если деплой успешен, ваша игра будет доступна на Vercel

---

## 📝 Рекомендации

1. **Используйте Vercel или Netlify** - самый простой вариант
2. **Настройте автоматический деплой** - при каждом push в main
3. **Используйте CDN** - для быстрой загрузки по всему миру
4. **Мониторьте производительность** - используйте аналитику
5. **Тестируйте на разных устройствах** - перед публикацией

---

**Готово!** Ваша игра будет доступна по всему миру! 🌍

