# 🔧 Настройка DevTools MCP Server v0.11.0

Это руководство поможет вам настроить DevTools MCP Server для работы с Cursor и другими MCP-клиентами.

## 📋 Что такое DevTools MCP Server?

DevTools MCP Server позволяет AI-ассистентам (например, Cursor) взаимодействовать с Chrome DevTools через Model Context Protocol. Это дает возможность:

- ✅ Ссылаться на элементы и сетевые запросы, выбранные на панелях «Элементы» и «Сеть»
- ✅ Получать сообщения консоли и проблемы из панели «Проблемы»
- ✅ Отлаживать события клавиатуры через инструмент `press_key`
- ✅ Сохранять снимки дерева доступности на диск
- ✅ Перезагружать страницы с игнорированием кэша
- ✅ Использовать существующий профиль Chrome через `--user-data-dir`

## 🚀 Быстрая настройка

### Шаг 1: Установка зависимостей

```bash
npm install
```

Это установит `@modelcontextprotocol/server-devtools@0.11.0` в devDependencies.

### Шаг 2: Запуск Chrome с поддержкой удаленной отладки

В отдельном терминале выполните:

```bash
npm run chrome:debug
```

Этот скрипт:
- Запустит Chrome с включенной поддержкой удаленной отладки на порту 9222
- Создаст временный профиль для отладки
- Автоматически откроет ваш dev-сервер (http://localhost:5173)

**Альтернативно**, вы можете запустить Chrome вручную:

**Windows:**
```bash
"C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222 --user-data-dir="%TEMP%\chrome-debug-profile"
```

**macOS:**
```bash
/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug-profile
```

**Linux:**
```bash
google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug-profile
```

### Шаг 3: Настройка Cursor

#### Вариант A: Через настройки Cursor (рекомендуется)

1. Откройте настройки Cursor (Ctrl+, или Cmd+,)
2. Найдите раздел "MCP Servers" или "Model Context Protocol"
3. Добавьте новый сервер со следующими параметрами:

```json
{
  "mcpServers": {
    "devtools": {
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-devtools@0.11.0",
        "--browser-url=http://localhost:9222"
      ],
      "env": {
        "CHROME_DEBUG_PORT": "9222"
      }
    }
  }
}
```

#### Вариант B: Через файл конфигурации

Скопируйте содержимое файла `mcp-config.json` в настройки MCP вашего Cursor.

**Расположение файла настроек Cursor:**
- **Windows:** `%APPDATA%\Cursor\User\globalStorage\mcp.json`
- **macOS:** `~/Library/Application Support/Cursor/User/globalStorage/mcp.json`
- **Linux:** `~/.config/Cursor/User/globalStorage/mcp.json`

### Шаг 4: Запуск DevTools MCP Server

После настройки Cursor, сервер должен автоматически запускаться при использовании MCP-функций.

**Вручную** (для тестирования):

```bash
npm run mcp:devtools
```

## 🎯 Использование

После настройки вы можете использовать DevTools MCP в Cursor:

### Примеры запросов:

1. **Проверка производительности:**
   ```
   Проверь производительность текущей страницы
   ```

2. **Анализ элементов:**
   ```
   Проанализируй выбранный элемент в DevTools
   ```

3. **Проверка сетевых запросов:**
   ```
   Покажи все сетевые запросы и их статусы
   ```

4. **Отладка проблем:**
   ```
   Покажи все проблемы из панели Issues
   ```

5. **Сохранение снимка доступности:**
   ```
   Сохрани снимок дерева доступности на диск
   ```

## 🔧 Дополнительные настройки

### Использование существующего профиля Chrome

Если вы хотите использовать свой обычный профиль Chrome:

1. Найдите путь к вашему профилю:
   - **Windows:** `%LOCALAPPDATA%\Google\Chrome\User Data\Default`
   - **macOS:** `~/Library/Application Support/Google/Chrome/Default`
   - **Linux:** `~/.config/google-chrome/Default`

2. Запустите Chrome с этим профилем:
   ```bash
   chrome.exe --remote-debugging-port=9222 --user-data-dir="C:\Users\YourName\AppData\Local\Google\Chrome\User Data"
   ```

3. Обновите конфигурацию MCP, добавив параметр `--user-data-dir` в args.

### Переменные окружения

Вы можете настроить поведение через переменные окружения:

- `CHROME_DEBUG_PORT` - Порт для удаленной отладки (по умолчанию: 9222)
- `CHROME_USER_DATA_DIR` - Путь к профилю Chrome
- `VITE_DEV_URL` - URL dev-сервера для автоматического открытия

Пример:
```bash
CHROME_DEBUG_PORT=9223 npm run chrome:debug
```

## 🐛 Устранение неполадок

### Chrome не запускается

- Убедитесь, что Chrome установлен
- Проверьте, что путь к Chrome правильный
- Попробуйте запустить Chrome вручную с теми же параметрами

### MCP Server не подключается

- Убедитесь, что Chrome запущен с `--remote-debugging-port=9222`
- Проверьте, что порт 9222 не занят другим процессом
- Убедитесь, что в конфигурации MCP указан правильный URL: `http://localhost:9222`

### Cursor не видит MCP Server

- Перезапустите Cursor после изменения конфигурации
- Проверьте, что конфигурация MCP в правильном формате JSON
- Убедитесь, что `@modelcontextprotocol/server-devtools` установлен

### Проблемы с безопасностью

⚠️ **Важно:** Remote debugging открывает Chrome для удаленного доступа. Используйте это только в безопасной среде разработки.

Для продакшена:
- Не запускайте Chrome с remote debugging на публичных серверах
- Используйте отдельный профиль для отладки
- Закрывайте Chrome с remote debugging после работы

## 📚 Дополнительные ресурсы

- [Официальный репозиторий DevTools MCP](https://github.com/ChromeDevTools/chrome-devtools-mcp)
- [Документация Model Context Protocol](https://modelcontextprotocol.io/)
- [Chrome DevTools Protocol](https://chromedevtools.github.io/devtools-protocol/)

## ✅ Проверка работы

После настройки выполните в Cursor:

```
Проверь, работает ли DevTools MCP Server. Покажи список доступных инструментов.
```

Если все настроено правильно, вы получите список доступных инструментов DevTools MCP.

---

**Версия:** 0.11.0  
**Последнее обновление:** 2024-12-XX

