# Улучшения генераторов карт (2025-12-28)

## Обзор

В версии от 28 декабря 2025 года были значительно улучшены генераторы карт, добавлены новые алгоритмы шума и оптимизирована производительность генерации террейна.

## 🎯 NoiseGenerator - Расширенные алгоритмы шума

### Новые методы

#### 1. `smoothFbm()` - Плавный шум
```typescript
smoothFbm(x: number, y: number, octaves: number = 4, lacunarity: number = 2.0, persistence: number = 0.5): number
```
- **Назначение**: Генерация более плавного шума с улучшенным качеством
- **Особенности**: Использует smoothstep для плавных переходов
- **Применение**: Биом `park` для органичных холмов

#### 2. `enhancedRidged()` - Улучшенные гребни
```typescript
enhancedRidged(x: number, y: number, octaves: number = 4, lacunarity: number = 2.0, gain: number = 0.5): number
```
- **Назначение**: Генерация более детализированных гребней для гор и каньонов
- **Особенности**: Добавляет детализацию с более высокими частотами
- **Применение**: Биомы `wasteland`, `park`, `snow` для горных пиков и каньонов

#### 3. `enhancedTurbulence()` - Сложные паттерны турбулентности
```typescript
enhancedTurbulence(x: number, y: number, octaves: number = 4): number
```
- **Назначение**: Генерация более сложных паттернов турбулентности
- **Особенности**: Добавляет вращение для более органичных паттернов
- **Применение**: Биом `wasteland` для кратеров и разрушенных зон

#### 4. `domainWarp()` - Деформация домена
```typescript
domainWarp(x: number, y: number, strength: number = 1.0): { x: number; y: number }
```
- **Назначение**: Создание более сложных паттернов через деформацию координат
- **Особенности**: Возвращает деформированные координаты для последующего использования
- **Применение**: Готов к использованию в будущих улучшениях

#### 5. `voronoi()` - Voronoi-шум
```typescript
voronoi(x: number, y: number, cellSize: number = 10): number
```
- **Назначение**: Создание ячеистых структур (кратеры, взрывы, клетки)
- **Особенности**: Генерирует паттерны на основе расстояния до ближайших точек
- **Применение**: Биом `wasteland` для ячеистых структур от взрывов

### Улучшения существующих методов

#### `fbm()` - Fractal Brownian Motion
- ✅ Поддержка до 8 октав (было 4)
- ✅ Улучшена нормализация результатов
- ✅ Защита от деления на ноль

#### `ridged()` - Ridged multifractal
- ✅ Поддержка до 8 октав
- ✅ Улучшена точность вычислений

#### `turbulence()` - Турбулентность
- ✅ Поддержка до 8 октав
- ✅ Улучшена нормализация

## 🗺️ TerrainGenerator - Улучшенная генерация террейна

### Использование новых методов в биомах

#### Биом `wasteland`
```typescript
// Использует enhancedTurbulence для кратеров
const craters = this.noise.enhancedTurbulence(worldX * scale * 2, worldZ * scale * 2, 4) * 12;

// Использует enhancedRidged для каньонов
const canyons = this.noise.enhancedRidged(worldX * scale * 1.2, worldZ * scale * 1.2, 3, 2, 0.6) * 10;

// Добавляет voronoi для ячеистых структур
const voronoiPattern = this.noise.voronoi(worldX * scale * 0.5, worldZ * scale * 0.5, 20) * 2;
```

#### Биом `park`
```typescript
// Использует smoothFbm для базового рельефа
const parkBase = this.noise.smoothFbm(worldX * scale, worldZ * scale, 5, 2, 0.5) * 20;

// Использует enhancedRidged для гребней
const parkRidges = this.noise.enhancedRidged(worldX * scale * 0.6, worldZ * scale * 0.6, 3, 2, 0.5) * 6;
```

#### Биом `snow`
```typescript
// Использует enhancedRidged для горных пиков
const mountains = this.noise.enhancedRidged(worldX * scale, worldZ * scale, 6, 2, 0.6) * 35;
```

### Результаты улучшений

- **Более детализированный террейн** - Многослойная генерация с улучшенными алгоритмами
- **Больше разнообразия** - Разные биомы используют специализированные методы
- **Лучшая производительность** - Оптимизированные алгоритмы с ограничением октав

## 🛠️ BaseMapGenerator - Расширенные возможности

### Новые вспомогательные методы

#### 1. `createObjectGroup<T>()`
```typescript
protected createObjectGroup<T>(
    count: number,
    createFn: (pos: Vector3, random: SeededRandom) => T,
    context: ChunkGenerationContext,
    minDistance: number = 5
): T[]
```
- **Назначение**: Создание групп объектов с вариациями позиций
- **Особенности**: Автоматическая проверка расстояний и размещения

#### 2. `applyRandomRotation()`
```typescript
protected applyRandomRotation(mesh: Mesh, random: SeededRandom, allowFullRotation: boolean = false): void
```
- **Назначение**: Случайный поворот объектов
- **Особенности**: Поддержка полного поворота (360°) или только 90° шагов

#### 3. `getVariedSize()`
```typescript
protected getVariedSize(baseSize: number, variation: number, random: SeededRandom): number
```
- **Назначение**: Генерация вариаций размера объектов
- **Особенности**: Вариация в диапазоне от `-variation` до `+variation`

#### 4. `canPlaceObject()`
```typescript
protected canPlaceObject(x: number, z: number, radius: number): boolean
```
- **Назначение**: Проверка возможности размещения объекта
- **Особенности**: Проверяет гаражи и дороги

#### 5. `createVariedMaterial()`
```typescript
protected createVariedMaterial(
    name: string,
    baseColor: Color3,
    variation: number,
    random: SeededRandom
): StandardMaterial
```
- **Назначение**: Создание материалов с вариациями цвета
- **Особенности**: Генерирует материалы с небольшими вариациями базового цвета

#### 6. `createLODGroup()`
```typescript
protected createLODGroup(
    nearObjects: Mesh[],
    farObjects: Mesh[],
    distance: number = 100
): { near: Mesh[]; far: Mesh[]; distance: number }
```
- **Назначение**: Создание групп LOD для оптимизации рендеринга
- **Особенности**: Разные уровни детализации для близких и дальних объектов

## 📦 CoverGenerator - Улучшенная генерация укрытий

### Улучшения

#### Вариации размеров контейнеров
```typescript
// УЛУЧШЕНО: Standard container dimensions с вариациями
const baseWidth = random.chance(0.3) ? 12 : 6; // 20ft or 40ft container
const sizeVariation = random.range(0.9, 1.1); // ±10% вариация
const width = baseWidth * sizeVariation;
const height = 2.6 * sizeVariation;
const depth = 2.4 * sizeVariation;
```

#### Больше вариантов укрытий
- Увеличен выбор укрытий для городских биомов
- Больше разнообразия в типах объектов

## ⚡ ChunkSystem - Оптимизация производительности

### Улучшения кэширования

#### Увеличенный кэш биомов
```typescript
private static readonly MAX_BIOME_CACHE_SIZE = 50000; // УВЕЛИЧЕНО для лучшего кэширования
```

#### Кэш для проверки позиций гаража
```typescript
private garageAreaCache: Map<string, boolean> = new Map();
private static readonly MAX_GARAGE_CACHE_SIZE = 10000;
```

#### Улучшена постобработка чанков
```typescript
const edgeSmoothingRadius = 3; // УВЕЛИЧЕНО с 2 до 3 для более агрессивного сглаживания
```

## 📊 Статистика улучшений

| Категория | Изменения |
|-----------|-----------|
| Новые методы NoiseGenerator | 5 методов |
| Новые методы BaseMapGenerator | 6 методов |
| Улучшенные биомы | 3 биома (wasteland, park, snow) |
| Оптимизации кэширования | 3 улучшения |
| Вариации объектов | Контейнеры ±10% |

## 🎮 Использование в игре

Все улучшения автоматически применяются при генерации карт. Нет необходимости в дополнительной настройке - просто играйте и наслаждайтесь улучшенным террейном!

### Визуальные улучшения

- **Более детализированные горы** - Использование `enhancedRidged` для снежных биомов
- **Реалистичные кратеры** - Использование `enhancedTurbulence` и `voronoi` для пустошей
- **Плавные холмы** - Использование `smoothFbm` для парков
- **Разнообразие объектов** - Вариации размеров и цветов

### Производительность

- **Улучшенное кэширование** - Быстрее генерация повторяющихся элементов
- **Оптимизированные алгоритмы** - Ограничение октав для производительности
- **LOD группы** - Готовность к оптимизации рендеринга дальних объектов

## 🔮 Будущие улучшения

Планируется:
- Использование `domainWarp` для более органичных структур
- Расширение использования `voronoi` для других биомов
- Дополнительные методы генерации для специфических структур
- Оптимизация производительности для больших карт

---

**Дата обновления**: 2025-12-28  
**Версия**: v0.4.20004+

