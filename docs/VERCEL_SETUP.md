# Настройка автоматического деплоя на Vercel

## ⚠️ ВАЖНО: Безопасность API ключей

**НЕ ПУБЛИКУЙТЕ API ключи в открытом доступе!** Если вы случайно опубликовали ключ:
1. Немедленно отзовите его на https://vercel.com/account/tokens
2. Создайте новый ключ
3. Добавьте новый ключ в GitHub Secrets

## 📋 Пошаговая инструкция

### Шаг 1: Получите Project ID и Org ID

1. Зайдите на https://vercel.com
2. Откройте ваш проект `protocol-xt`
3. Перейдите в **Settings** → **General**
4. Найдите следующие значения:
   - **Project ID** (например: `prj_xxxxxxxxxxxxx`)
   - **Team ID** или **Org ID** (например: `team_xxxxxxxxxxxxx` или `user_xxxxxxxxxxxxx`)

### Шаг 2: Добавьте секреты в GitHub

1. Зайдите в ваш репозиторий на GitHub: https://github.com/AdotEXE/protocol-XT
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте три секрета:

   **Секрет 1:**
   - **Name**: `VERCEL_TOKEN`
   - **Secret**: `vck_7MIrOjTHRX4AaG3AIySIlcxRzV7lUcwB8Rjv7aKAxeddKjdx3341mS0B`
   - Нажмите **Add secret**

   **Секрет 2:**
   - **Name**: `VERCEL_ORG_ID`
   - **Secret**: (ваш Team ID или Org ID из шага 1)
   - Нажмите **Add secret**

   **Секрет 3:**
   - **Name**: `VERCEL_PROJECT_ID`
   - **Secret**: (ваш Project ID из шага 1)
   - Нажмите **Add secret**

### Шаг 3: Проверьте автоматический деплой

1. После добавления секретов, следующий push в ветку `main` автоматически запустит деплой
2. Проверьте статус в **Actions** вкладке на GitHub
3. Если деплой успешен, ваша игра будет автоматически обновляться на https://protocol-xt.vercel.app/

## 🔍 Как найти Project ID и Org ID

### Через веб-интерфейс Vercel:

1. Зайдите на https://vercel.com
2. Откройте проект `protocol-xt`
3. **Settings** → **General**
4. Прокрутите вниз до раздела **Project ID** и **Team ID**

### Через Vercel CLI:

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в Vercel
vercel login

# Получите информацию о проекте
vercel inspect protocol-xt
```

## ✅ Проверка настройки

После добавления секретов:

1. Сделайте любой коммит и push в `main`:
   ```bash
   git commit --allow-empty -m "test: Проверка автоматического деплоя"
   git push origin main
   ```

2. Зайдите в **Actions** на GitHub
3. Дождитесь завершения workflow
4. Проверьте, что шаг "Deploy to Vercel" выполнен успешно

## 🐛 Решение проблем

### Ошибка: "Input required and not supplied: vercel-token"

**Причина**: Секреты не добавлены или названы неправильно

**Решение**:
1. Проверьте, что секреты добавлены в **Settings** → **Secrets and variables** → **Actions**
2. Убедитесь, что имена секретов **точно**: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`
3. Регистр важен! Все буквы должны быть заглавными

### Ошибка: "Project not found"

**Причина**: Неправильный Project ID или Org ID

**Решение**:
1. Проверьте Project ID и Org ID в настройках Vercel
2. Убедитесь, что вы скопировали полные значения (включая префиксы `prj_` и `team_`/`user_`)

### Деплой не запускается автоматически

**Причина**: Workflow не настроен или секреты не добавлены

**Решение**:
1. Проверьте, что файл `.github/workflows/deploy-vercel.yml` существует
2. Убедитесь, что секреты добавлены
3. Попробуйте перезапустить workflow вручную: **Actions** → выберите workflow → **Re-run all jobs**

## 🔒 Безопасность

- ✅ **НЕ** коммитьте API ключи в код
- ✅ **НЕ** публикуйте API ключи в открытом доступе
- ✅ Используйте **GitHub Secrets** для хранения ключей
- ✅ Регулярно обновляйте API ключи
- ✅ Отзывайте старые ключи, если они были скомпрометированы

## 📚 Дополнительная информация

- [Vercel Documentation](https://vercel.com/docs)
- [GitHub Actions Secrets](https://docs.github.com/en/actions/security-guides/encrypted-secrets)
- [Vercel API Tokens](https://vercel.com/account/tokens)

---

**Готово!** Теперь каждый push в `main` будет автоматически деплоить вашу игру на Vercel! 🚀

