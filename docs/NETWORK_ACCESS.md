# 🌐 Доступ к игре из локальной сети

## Проблема

По умолчанию Vite dev server слушает только на `127.0.0.1` (localhost), что означает, что он доступен только на локальной машине. Другие компьютеры в сети не могут подключиться.

## Решение

### 1. Изменение конфигурации Vite

В `vite.config.ts` изменён `host` с `'127.0.0.1'` на `'0.0.0.0'`:

```typescript
server: {
  port: 5000,
  host: '0.0.0.0', // Слушаем на всех интерфейсах
  // ...
}
```

### 2. Автоматическое определение IP-адреса

**Все скрипты запуска теперь автоматически показывают сетевые IP-адреса!**

При запуске любого из следующих скриптов вы увидите информацию о сетевом доступе:
- `npm run dev:go` - Unified Dashboard
- `npm run dev:unified` - Unified Dashboard (TypeScript)
- `npm run dev:simple` - Simple Dashboard
- `npm run server:start` - Запуск сервера
- `npm run dev` - Vite dev server (в консоли)

Все скрипты автоматически определяют ваш локальный IP-адрес и показывают:
- 📍 Локальный доступ (localhost)
- 🌐 Сетевой доступ (для других ПК в сети)
- 📡 Все доступные IP-адреса (если их несколько)

#### Ручное определение IP-адреса (если нужно):

**Windows:**
```powershell
# PowerShell
ipconfig | findstr /i "IPv4"

# Или более детально:
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -notlike '127.*'}
```

**Linux/Mac:**
```bash
# Linux
ip addr show | grep "inet " | grep -v 127.0.0.1

# Mac
ifconfig | grep "inet " | grep -v 127.0.0.1
```

### 3. Доступ с другого компьютера

После изменения конфигурации и перезапуска dev сервера:

1. **Найдите ваш локальный IP-адрес** (например, `192.168.1.100`)
2. **На другом компьютере** откройте браузер и перейдите по адресу:
   ```
   http://192.168.1.100:5000
   ```

### 4. Проверка брандмауэра

Если другой компьютер всё ещё не может подключиться:

#### Windows:
1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Разрешить взаимодействие с приложением..."
3. Найдите "Node.js" или добавьте правило для порта 5000

#### Или через PowerShell (от имени администратора):
```powershell
New-NetFirewallRule -DisplayName "Vite Dev Server" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
```

### 5. Безопасность

⚠️ **Важно**: `0.0.0.0` делает сервер доступным для всех в вашей сети. Это нормально для разработки, но:

- Используйте только в доверенной сети (домашняя/офисная)
- Не используйте в публичных сетях (кафе, аэропорт и т.д.)
- Для production используйте правильную конфигурацию с ограничением доступа

### 6. Альтернатива: только для разработки

Если вы хотите, чтобы сервер был доступен из сети только иногда, можно использовать переменную окружения:

```typescript
// vite.config.ts
server: {
  port: 5000,
  host: process.env.VITE_HOST || '127.0.0.1',
  // ...
}
```

Затем запускайте:
```bash
# Локально (только на этой машине)
npm run dev

# Из сети (доступен другим компьютерам)
VITE_HOST=0.0.0.0 npm run dev
```

## Примеры IP-адресов

- `127.0.0.1` или `localhost` - только локальная машина
- `0.0.0.0` - все сетевые интерфейсы (доступен из сети)
- `192.168.1.100` - конкретный IP в локальной сети
- `10.0.0.5` - IP в другой подсети

## Проверка подключения

После настройки проверьте:

1. **На сервере** (где запущен dev):
   ```bash
   # Должен показать, что слушает на 0.0.0.0:5000
   netstat -an | findstr :5000
   ```

2. **На клиенте** (другой компьютер):
   ```bash
   # Проверка доступности
   ping <IP-адрес-сервера>
   
   # Или в браузере
   http://<IP-адрес-сервера>:5000
   ```

## Troubleshooting

### Проблема: "Connection refused"
- Проверьте, что сервер запущен
- Проверьте, что `host: '0.0.0.0'` в конфиге
- Проверьте брандмауэр

### Проблема: "Timeout"
- Проверьте, что оба компьютера в одной сети
- Проверьте IP-адрес сервера
- Проверьте, что порт 5000 не заблокирован

### Проблема: "CORS error"
- Vite автоматически обрабатывает CORS для dev сервера
- Если проблема остаётся, проверьте настройки прокси в `vite.config.ts`

