# Руководство по настройке окружения разработки

## 📋 Требования

### Обязательные

- **Node.js** 20+ (LTS версия)
  - Скачать: https://nodejs.org/
  - Проверить: `node --version`

- **npm** или **yarn**
  - npm идет вместе с Node.js
  - Проверить: `npm --version`

- **Git**
  - Скачать: https://git-scm.com/
  - Проверить: `git --version`

### Рекомендуемые

- **VS Code** - Редактор кода
  - Скачать: https://code.visualstudio.com/
  - Рекомендуемые расширения:
    - TypeScript
    - ESLint
    - Prettier

- **Chrome DevTools** - Для отладки
  - Встроен в Chrome/Edge

## 🚀 Быстрая установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/AdotEXE/protocol-XT.git
cd protocol-XT
```

### 2. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты, включая:
- Babylon.js и его плагины
- Havok Physics
- TypeScript
- Vite и инструменты сборки

### 3. Запуск в режиме разработки

#### Вариант 1: Обычный запуск

```bash
npm run dev
```

Игра будет доступна по адресу: `http://localhost:3000`

#### Вариант 2: Запуск через мониторинг (рекомендуется)

Система мониторинга автоматически откроет все необходимые окна терминала:

```bash
# Запуск всех систем одновременно (сервер, клиент, мониторинг)
npm run monitor

# Или только мониторинг (откроет все окна автоматически)
npm run monitor:only
```

Система откроет 4 окна терминала в сетке 2x2:
- **Верхний левый**: Мониторинг (дашборд с метриками)
- **Верхний правый**: Сервер (WebSocket на порту 8000)
- **Нижний левый**: Клиент (Vite dev server на порту 3000)
- **Нижний правый**: Системные логи

Подробнее: [docs/MONITORING.md](MONITORING.md)

## 🔧 Детальная настройка

### Настройка VS Code

#### Рекомендуемые расширения

1. **TypeScript** - Поддержка TypeScript
2. **ESLint** - Линтинг кода
3. **Prettier** - Форматирование кода
4. **GitLens** - Улучшенная работа с Git

#### Настройки workspace

Создайте `.vscode/settings.json`:

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "typescript.tsdk": "node_modules/typescript/lib",
  "files.exclude": {
    "**/node_modules": true,
    "**/dist": true
  }
}
```

### Настройка Git

#### Первоначальная настройка

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

#### Настройка SSH ключей (опционально)

1. Создайте SSH ключ:
```bash
ssh-keygen -t ed25519 -C "ваш@email.com"
```

2. Добавьте ключ в GitHub:
   - Скопируйте содержимое `~/.ssh/id_ed25519.pub`
   - GitHub → Settings → SSH and GPG keys → New SSH key

### Настройка Firebase (опционально)

Если вы хотите работать с Firebase:

1. Установите Firebase CLI:
```bash
npm install -g firebase-tools
```

2. Войдите в Firebase:
```bash
firebase login
```

3. Инициализируйте проект:
```bash
firebase init
```

## 🛠️ Скрипты разработки

### Основные команды

#### Мониторинг и запуск систем

```bash
# Запуск всех систем одновременно (сервер, клиент, мониторинг)
npm run monitor

# Только мониторинг (откроет все окна автоматически)
npm run monitor:only

# Системные логи
npm run system:logs

# Запуск всех систем последовательно
npm run start:all

# Перезапуск всех систем
npm run restart:all
```

Подробнее о мониторинге: [docs/MONITORING.md](MONITORING.md)

#### Разработка

```bash
# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build

# Просмотр продакшен-сборки
npm run preview
```

### Дополнительные команды

```bash
# Проверка типов TypeScript
npx tsc --noEmit

# Линтинг кода (если настроен)
npm run lint

# Форматирование кода (если настроен)
npm run format
```

### Команды мониторинга

```bash
# Запуск всех систем одновременно (сервер, клиент, мониторинг)
npm run monitor

# Только мониторинг (откроет все окна автоматически)
npm run monitor:only

# Системные логи
npm run system:logs

# Запуск всех систем последовательно
npm run start:all

# Перезапуск всех систем
npm run restart:all
```

Подробнее о мониторинге: [docs/MONITORING.md](MONITORING.md)

## 🐛 Отладка

### Отладка в браузере

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Sources"
3. Найдите файлы в `src/client/`
4. Установите breakpoints

### Отладка в VS Code

1. Создайте `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Launch Chrome",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
```

2. Нажмите F5 для запуска отладки

### Профилирование производительности

1. Откройте DevTools (F12)
2. Перейдите на вкладку "Performance"
3. Нажмите "Record"
4. Играйте в игру
5. Остановите запись
6. Анализируйте результаты

## 📦 Структура проекта

```
TX/
├── src/
│   ├── client/          # Клиентский код
│   ├── server/          # Серверный код (в разработке)
│   └── shared/          # Общий код
├── public/              # Статические файлы
├── docs/                # Документация
├── node_modules/        # Зависимости (не коммитить)
├── dist/                # Собранные файлы (не коммитить)
├── package.json         # Зависимости и скрипты
├── tsconfig.json        # Конфигурация TypeScript
├── vite.config.ts       # Конфигурация Vite
└── .gitignore          # Игнорируемые файлы
```

## 🔍 Проверка установки

### Проверка Node.js

```bash
node --version
# Должно быть: v20.x.x или выше
```

### Проверка npm

```bash
npm --version
# Должно быть: 10.x.x или выше
```

### Проверка зависимостей

```bash
npm list --depth=0
# Должны быть установлены все зависимости из package.json
```

### Проверка сборки

```bash
npm run build
# Должно завершиться без ошибок
```

## 🚨 Решение проблем

### Проблемы с установкой зависимостей

**Ошибка**: `npm ERR! code ERESOLVE`

**Решение**:
```bash
npm install --legacy-peer-deps
```

**Ошибка**: `Cannot find module`

**Решение**:
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портом

**Ошибка**: `Port 3000 is already in use`

**Решение**:
1. Найдите процесс, использующий порт:
```bash
# Windows
netstat -ano | findstr :3000

# Linux/Mac
lsof -i :3000
```

2. Закройте процесс или измените порт в `vite.config.ts`

### Проблемы с WebGPU

**Ошибка**: WebGPU not supported

**Решение**:
- Обновите браузер до последней версии
- Chrome 113+ или Edge 113+ поддерживают WebGPU
- Игра автоматически переключится на WebGL2 если WebGPU недоступен

## 📚 Дополнительные ресурсы

- [Babylon.js Documentation](https://doc.babylonjs.com/)
- [Havok Physics](https://www.havok.com/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Vite Guide](https://vitejs.dev/guide/)

## ✅ Чеклист настройки

- [ ] Node.js 20+ установлен
- [ ] npm установлен
- [ ] Git настроен
- [ ] Репозиторий клонирован
- [ ] Зависимости установлены (`npm install`)
- [ ] Проект запускается (`npm run dev`)
- [ ] Игра открывается в браузере
- [ ] Нет ошибок в консоли
- [ ] VS Code настроен (опционально)

---

**Готово!** Теперь вы можете начать разработку. См. [CONTRIBUTING.md](../CONTRIBUTING.md) для руководства по контрибьютингу.

