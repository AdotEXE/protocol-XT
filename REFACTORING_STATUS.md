# Статус рефакторинга garage.ts

## ✅ Выполнено:

1. **Добавлены импорты новых модулей:**
   - `MaterialFactory` 
   - `ChassisDetailsGenerator`
   - `CannonDetailsGenerator`

2. **Заменено создание базовых материалов:**
   - `armorMat` → `MaterialFactory.createArmorMaterial()`
   - `accentMat` → `MaterialFactory.createAccentMaterial()`

3. **Частично отрефакторены детали корпусов:**
   - ✅ **"light"** - полностью переведен на использование модулей
   - ✅ **"scout"** - частично переведен (основные детали)
   - ⚠️ **Остальные 13 корпусов** - еще не отрефакторены

4. **Детали пушек:**
   - ❌ Метод `createUniqueCannonPreview()` - еще не использует `CannonDetailsGenerator`

## 📊 Статистика:

- **Всего вызовов** `new StandardMaterial|MeshBuilder.Create`: **392**
- **Использований новых модулей**: **31**
- **Процент завершенности**: ~8%

## 🔄 Что нужно завершить:

### 1. Рефакторинг деталей корпусов (13 корпусов):

Осталось отрефакторить детали для корпусов:
- [ ] `medium` - Т-34 стиль
- [ ] `racer` - спортивный стиль
- [ ] `stealth` - скрытность
- [ ] `destroyer` - танк-истребитель
- [ ] `heavy` - тяжелый (ИС-2/ИС-7)
- [ ] `assault` - штурмовой
- [ ] `hover` - ховер
- [ ] `siege` - осадный
- [ ] `amphibious` - амфибия
- [ ] `shield` - щитовой
- [ ] `drone` - дроновый
- [ ] `artillery` - артиллерийский
- [ ] `command` - командный

### 2. Рефакторинг деталей пушек:

Метод `createUniqueCannonPreview()` нужно перевести на использование `CannonDetailsGenerator`:

Доступные функции в `CannonDetailsGenerator`:
- `createScope()` - прицел
- `createBipod()` - сошка
- `createStabilizer()` - стабилизатор
- `createMiniBarrel()` - мини-ствол (для gatling)
- `createCoolingRing()` - кольцо охлаждения
- `createBreech()` - затвор
- `createMuzzleBrake()` - дульный тормоз
- `createPlasmaCore()` - плазменное ядро
- `createPlasmaCoil()` - плазменная катушка
- `createPlasmaStabilizer()` - плазменный стабилизатор
- `createPlasmaEmitter()` - плазменный эмиттер
- `createLens()` - линза
- `createFocusRing()` - кольцо фокусировки
- `createLaserChannel()` - лазерный канал
- `createLaserHousing()` - корпус лазера
- `createRail()` - рельс
- `createCapacitor()` - конденсатор
- `createRailChannel()` - рельсовый канал
- `createMuzzleAmplifier()` - усилитель дула
- `createTeslaCoil()` - катушка Тесла
- `createTeslaDischarger()` - разрядник Тесла
- `createTeslaGenerator()` - генератор Тесла
- `createGuide()` - направляющая
- `createRocketFin()` - стабилизатор ракеты
- `createRocketGuidance()` - система наведения
- `createRocketTube()` - труба ракеты
- `createPelletBarrel()` - ствол для дроби
- `createCenterBarrel()` - центральный ствол
- `createShotgunReinforcement()` - усиление дробовика

Типы пушек, которые нужно отрефакторить:
- [ ] `sniper`
- [ ] `gatling`
- [ ] `plasma`
- [ ] `laser`
- [ ] `railgun`
- [ ] `tesla`
- [ ] `rocket`
- [ ] `shotgun`
- [ ] `artillery`
- [ ] `flame`
- и другие...

## 📝 Приоритеты:

1. **Высокий приоритет:**
   - Завершить рефакторинг для популярных корпусов (`medium`, `heavy`, `assault`)
   - Рефакторинг пушек для основных типов (`sniper`, `gatling`, `plasma`)

2. **Средний приоритет:**
   - Рефакторинг остальных корпусов
   - Рефакторинг остальных типов пушек

3. **Низкий приоритет:**
   - Оптимизация и очистка кода после рефакторинга
   - Документация изменений

## 🎯 План действий:

1. Продолжить рефакторинг корпусов по очереди (можно делать батчами)
2. Рефакторить пушки после завершения корпусов
3. Протестировать каждый шаг рефакторинга
4. Коммитить изменения постепенно
